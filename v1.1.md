# V1.1 Overlay Evidence And Replay Plan

Working note from the 2026-05-22 Dallara P217 LMP2 / Gesamtstrecke Long capture and diagnostics deep-dive.

This file is intentionally evidence-oriented. It records what the current bundles prove, what they do not prove, and the V1.1 tool shape that would make this analysis repeatable instead of forensic.

## Collection Boundary

V1.1 high-fidelity evidence must stay opt-in.

On Windows, the app should only start raw capture and any V1.1 per-frame model/pixel replay sidecars after the user intentionally enables **Enhanced iRacing Telemetry Capture** in Support before joining/driving. The existing `TelemetryCapture:RawCaptureEnabled=true` configuration path can remain a developer/test-build escape hatch, but diagnostics should report that as the opt-in source. It should not look like silent default collection.

When enhanced capture is off, ordinary diagnostics can still collect bounded counters, route activity, compact overlay diagnostics, performance summaries, and final/last-active snapshots. They should not collect raw `telemetry.bin`, live per-frame overlay payloads, or live pixel/screenshot evidence as V1.1 artifacts.

The offline `overlay_forensics.py` path is different: it can analyze a `capture-*` directory that was already supplied. That is post-processing of an explicit capture, not a new background collection mechanism.

Storage rule:

- Raw session truth and live high-fidelity sidecars belong under the explicit `%LOCALAPPDATA%\TmrOverlay\captures\capture-*` folder that enhanced capture created.
- Diagnostics bundles under `%LOCALAPPDATA%\TmrOverlay\diagnostics` may include compact summaries, manifests, evidence-quality warnings, and references to the latest capture, but they should not become the primary store for raw telemetry or bulky V1.1 replay artifacts.
- App-owned forensics packages belong under `%LOCALAPPDATA%\TmrOverlay\forensics\<capture-id>`. Enhanced capture finalization creates the starter package automatically. The offline tool can enrich that package from the explicit `capture-*` input, while `--output artifacts/overlay-forensics/<capture-id>` remains useful for disposable dev-workspace experiments.

## Source Bundles

New raw capture folders:

- `capture-20260522-185231-444`
- `capture-20260522-191332-529`
- `capture-20260522-192333-050`
- `capture-20260522-194832-318`
- `capture-20260522-202455-020`
- `capture-20260522-204847-774`

New diagnostics folders:

- `dallara-p217-lmp2-gesamtstrecke-long-20260522-185010-048`
- `dallara-p217-lmp2-gesamtstrecke-long-20260522-190930-047`
- `dallara-p217-lmp2-gesamtstrecke-long-20260522-192120-985`
- `dallara-p217-lmp2-gesamtstrecke-long-20260522-194756-102`
- `dallara-p217-lmp2-gesamtstrecke-long-20260522-202258-315`
- `dallara-p217-lmp2-gesamtstrecke-long-20260522-204816-198`
- `dallara-p217-lmp2-gesamtstrecke-long-20260522-214620-532`

Best full-race evidence pairs:

- Race 1: `capture-20260522-194832-318` plus `dallara-p217-lmp2-gesamtstrecke-long-20260522-202258-315`
- Race 2: `capture-20260522-204847-774` plus `dallara-p217-lmp2-gesamtstrecke-long-20260522-214620-532`

Race 1 raw capture:

- Started `2026-05-22T19:48:32.3184684+00:00`, finished `2026-05-22T20:22:43.6229505+00:00`.
- `122998` frames at `60` Hz, `0` dropped frames, `164` session info snapshots, `336` variables.
- Race session, Dallara P217 LMP2, Gesamtstrecke Long, `4` race laps, player/focus car `17`, `51` drivers including pace car.

Race 2 raw capture:

- Started `2026-05-22T20:48:47.7741707+00:00`, finished `2026-05-22T21:46:05.3166784+00:00`.
- `206109` frames at `60` Hz, `0` dropped frames, `278` session info snapshots, `336` variables.
- Race session, Dallara P217 LMP2, Gesamtstrecke Long, `7` race laps, player/focus car `19`, `46` drivers including pace car.

Older teammate/team-racing captures to keep in the V1.1 replay corpus:

- 4h/VLN: `captures/capture-20260426-130334-932`, Gesamtstrecke VLN, `TeamRacing=1`, driver car `15`, `1036026` observed frames, `2208` session info updates, `0` dropped frames.
- 24h main stint: `captures/capture-20260502-143722-571`, Gesamtstrecke 24h, `TeamRacing=1`, driver car `43`, `277680` observed frames, `750` session info updates, `0` dropped frames.
- 24h continuation: `captures/capture-20260502-155431-647`, Gesamtstrecke 24h, `TeamRacing=1`, driver car `43`, `55975` observed frames even though its manifest says `frameCount=0`, `166` session info updates.
- 24h startup fragments: `captures/capture-20260502-141919-875` (`50` frames), `captures/capture-20260502-141936-220` (`9` frames), and `captures/capture-20260502-141939-725` (`1` frame). Keep these as capture-start/finalization edge cases, not as overlay-behavior evidence.
- GR86 spectated race: no raw `capture-*` folder in the current workspace has GR86/Toyota metadata. There are many historical `captures/IBT/toyotagr86_*.ibt` files, but `overlay_forensics.py` currently accepts raw capture directories, not standalone IBT files. V1.1 needs either the missing raw capture copied in or an IBT-to-forensics/import path before this can be included.

## Evidence Quality

The raw captures are the best source for what the telemetry said. The diagnostics bundles are mostly finalization snapshots plus counters.

Important limitation: both full-race diagnostics were created from `session_finalization` after the iRacing session ended. Current and last-active overlay model snapshots often say hidden, disconnected, stale, or telemetry unavailable. Those final states are not direct proof of what was rendered during the live race.

Live performance was captured, but the bundle summary hides most of the timeline:

- `metadata/performance.json` is a final aggregate snapshot.
- `performance/performance-20260522.jsonl` has continuous samples, roughly every 30 seconds.
- In race 2, performance samples run through the capture window and show capture progress reaching `206109` frames with pending message count `0`.

Pixel evidence is missing:

- `metadata/evidence-quality.json` warns `live_overlay_screenshot_capture_disabled`.
- It also warns `visible_overlays_without_pixel_evidence` and `visible_overlays_without_current_screenshots`.
- That means we can prove route/API behavior and raw model inputs, but not exact OBS pixels for each overlay.

V1.1 should treat this as a first-class product gap. Every live capture should preserve enough sampled live model and pixel evidence to answer: "what did each surface render, and did it match the raw telemetry at that moment?"

## OBS / Localhost Summary

Localhost itself was healthy in both full-race bundles.

Race 1:

- `totalRequests=114180`
- `successfulRequests=114180`
- `requestErrorCount=0`
- all status codes were `200`

Race 2:

- `totalRequests=157583`
- `successfulRequests=157583`
- `requestErrorCount=0`
- all status codes were `200`

OBS loaded and polled these overlays in both races:

- `car-radar`
- `fuel-calculator`
- `gap-to-leader`
- `garage-cover`
- `input-state`
- `relative`
- `session-weather`
- `stream-chat`
- `track-map`

OBS did not load these overlays in either full-race bundle:

- `standings`: `html=0`, `model=0`, `render=0`
- `flags`: `html=0`, `model=0`, `render=0`
- `pit-service`: `html=0`, `model=0`, `render=0`

That is not a route failure. It is scene/source absence, disabled route selection, or product exposure/configuration. V1.1 should report OBS scene/source coverage as a readiness failure when the capture has data for an overlay but OBS never requests it.

## V1.1 Tool Run - Full Race Overlay Matrix

Command shape used on 2026-05-22, evidence/index mode only because local `dotnet` model replay is not available on this Mac:

```bash
python3 tools/analysis/overlay_forensics.py \
  --capture capture-20260522-194832-318 \
  --diagnostics dallara-p217-lmp2-gesamtstrecke-long-20260522-202258-315 \
  --output artifacts/overlay-forensics/capture-20260522-194832-318 \
  --strategy issue-repro --model-replay off --render none

python3 tools/analysis/overlay_forensics.py \
  --capture capture-20260522-204847-774 \
  --diagnostics dallara-p217-lmp2-gesamtstrecke-long-20260522-214620-532 \
  --output artifacts/overlay-forensics/capture-20260522-204847-774 \
  --strategy issue-repro --model-replay off --render none
```

Generated packages:

- Race 1: `artifacts/overlay-forensics/capture-20260522-194832-318` with `93` indexed events, `143` sampled frames, and `1965` performance samples in the capture window.
- Race 2: `artifacts/overlay-forensics/capture-20260522-204847-774` with `93` indexed events, `216` sampled frames, and `2132` performance samples in the capture window.

Important boundary: these packages prove raw telemetry, route/model counters, page events, performance windows, and sampled-event plans. They still do not prove pixels because diagnostics had live screenshot capture disabled. They also do not replay the production C# overlay models yet because this run used `--model-replay off`.

| Overlay | Raw telemetry said | OBS / model behavior | Current classification | V1.1 assertion to add |
| --- | --- | --- | --- | --- |
| `standings` | Race 1 had scoring data on `122539` frames, max `50` rows, `3` class groups; Race 2 had scoring data on `205634` frames, max `45` rows, `3` class groups. Race 2 starts with Practice `SessionResults` rows already present. | OBS never requested the route in either race: `html=0`, `model=0`, `render=0`. Final model snapshots only say hidden/telemetry unavailable because the bundle was post-session. | OBS readiness/source missing, plus unresolved practice flashing/non-race semantics. | Fail when raw scoring exists but OBS never requests Standings; add replay windows around practice/result-source transitions and assert no race-only `GAP`/`INT`/leader semantics before a valid standings source exists. |
| `relative` | Race 1 had nearby cars on `122313` frames and official lap delta on `90362`; Race 2 had nearby cars on `205457` and official lap delta on `173069`. Lap delta quality was usable in both. | OBS requested and rendered: Race 1 `model=4573`, `render=353`; Race 2 `model=4657`, `render=432`. | Route/model coverage healthy; no pixel proof. | Preserve as a positive control for route/model coverage and lap-delta provenance; replay should compare visible row identity and focus context when model replay is available. |
| `gap-to-leader` | Race 1 had gap data on `90362` frames; Race 2 had gap data on `173545`. Race-projection sources changed during both captures and included non-race projection signals at start. | OBS requested and rendered: Race 1 `model=4501`, `render=167`; Race 2 `model=4585`, `render=215`. Paint was over budget: Race 1 `25.879ms` vs `16.667ms`; Race 2 `33.528ms` vs `16.667ms`. | Route/model coverage healthy; performance warning; pit-stop/long-tail scale issue not yet proven without replayed model/pixels. | Replay focus-relative scale, pit-stop windows, and far-away row compression; assert graph scale and top-car readability when final included lines are minutes behind. |
| `car-radar` | Race 1 side signal was sparse: `73` side-signal frames, `3` side-signal-without-placement frames, no multiclass approach frames. Race 2 had `565` side-signal frames, `167` side-signal-without-placement frames, and `221` multiclass approach frames. | OBS requested and rendered: Race 1 `model=10303`, `render=28`; Race 2 `model=10387`, `render=37`. Race 2 event at session time `255.667` shows raw `right` side signal with no placement candidate. | Route/model coverage healthy; Radar placement/source ambiguity; pixel proof missing. | Sample every `CarLeftRight` and multiclass-approach transition; classify warnings as side-signal-only, placement-only, or both; assert timing window for faster-class cars behind and require explicit behavior for side-without-placement. |
| `fuel-calculator` | Race 1 had fuel level on `118350` frames; Race 2 had fuel level on `202597`. Both runs reported `pitWindowsWithFuelIncrease=0` despite the earlier capture analysis showing real net fuel additions. | OBS requested and rendered: Race 1 `model=4350`, `render=113`; Race 2 `model=4434`, `render=185`. Final model snapshots are post-session waiting states. | Route/model coverage healthy; live measured-burn/refuel detection still suspect. | Replay completed-lap fuel-burn eligibility and pit-window fuel deltas; assert live measured burn becomes available when current-session laps support it and fuel increases are detected when net level rises. |
| `pit-service` | Same fuel/pit-service raw signals as Fuel: Race 1 `122998` pit-service signal frames; Race 2 `206109`; request frames on nearly the full capture. | OBS never requested Pit Service in either race: `html=0`, `model=0`, `render=0`. | OBS readiness/source missing, plus shared refuel-event diagnostic gap. | Fail when pit-service signals exist but OBS never requests Pit Service; replay pit command and pit-window transitions against rendered model rows. |
| `flags` | Race 1 confirms the meatball was real in raw/model diagnostics: `Meatball=90905` display-kind frames, longest display `Critical:Meatball` for `515.199s`. Race 2 had `White=28325`, `Checkered=13906`, `Black=862`, plus yellow/debris/green activity. | OBS never requested Flags in either race: `html=0`, `model=0`, `render=0`. Native performance still had visible-window samples, but no OBS source and no pixels. | OBS readiness/source missing; local-vs-global flag evidence still needs policy. | Preserve the confirmed real meatball; fail when flag data exists but OBS never requests Flags; add local-driver evidence checks before treating global black/furled/repair bits as local critical display. |
| `track-map` | Race 1 had sectors on all `122998` frames, live timing on `114471`, highlighted sectors on `1161`; Race 2 had sectors on all `206109`, live timing on `194866`, highlighted sectors on `7063`, best-lap sectors on `606`. | OBS requested and rendered: Race 1 `model=10778`, `render=268`; Race 2 `model=10862`, `render=347`. | Route/model coverage healthy; no pixel proof of generated-map vs fallback-map display. | Replay map-source selection and sector-highlight transitions; assert map kind/source/fallback reason and rendered marker/sector counts. |
| `input-state` | Race 1 had `103677` driver-control change frames; Race 2 had `184965`. Inputs were healthy. | OBS requested and rendered: Race 1 `model=22180`, `render=114`; Race 2 `model=22264`, `render=186`. | Route/model coverage healthy; no specific bug proven. | Keep as a positive local-input fixture; replay trace counts, latest scalar values, and graph/rail sync through model replay. |
| `session-weather` | Raw telemetry existed but the current tool only summarizes general raw telemetry here, not weather-specific semantics. | OBS requested and rendered: Race 1 `model=4218`, `render=253`; Race 2 `model=4302`, `render=332`. | Route/model coverage healthy; semantic evidence too shallow. | Add weather-specific evidence fields before using this overlay as proof: weather source, track temp/air temp/wetness samples, and stale/unavailable classification. |
| `garage-cover` | Sampled raw frames had `IsGarageVisible=false` throughout both full races, so these captures do not prove a live garage-screen transition. | OBS polled heavily but never rendered: Race 1 `model=26333`, `hidden=1398`, `render=0`; Race 2 `model=46348`, `hidden=2458`, `render=0`. Final model status was `disabled | product hidden`. | Product policy clarified after the Navarra OBS test: the cover should render only when the user-enabled Garage Cover sees fresh `IsGarageVisible=true`. | Replay enabled/disabled, stale/disconnected, garage-visible/garage-hidden, and image-ready/image-missing states; assert stock fallback art is used by default when no imported image is available. |
| `stream-chat` | No raw telemetry signal is expected. | OBS requested and rendered: Race 1 `model=22104`, `render=1201`; Race 2 `model=42197`, `render=2260`. Current final model still said `chat connected | twitch`. | Route/model coverage healthy; external chat content remains outside raw capture truth. | Treat as a route/provider health overlay; assert provider status, sanitized source state, row counts, and synthetic/external-source classification. |

## V1.1 Tool Run - Endurance Team Capture Sweep

Command shape used for the older team-racing captures:

```bash
python3 tools/analysis/overlay_forensics.py \
  --capture captures/<capture-id> \
  --output artifacts/overlay-forensics/<capture-id> \
  --strategy issue-repro --model-replay off --render none \
  --assert warn --fail-on none
```

Generated packages:

| Capture | Meaning | Observed frames | Indexed events | Sampled frames | Performance samples | Evidence value |
| --- | --- | ---: | ---: | ---: | ---: | --- |
| `capture-20260426-130334-932` | 4h/VLN team race | `1036026` | `12` | `497` | `0` | Strong raw capture duration and teammate/team-racing corpus candidate, but no live overlay sidecar. |
| `capture-20260502-143722-571` | 24h main team stint | `277680` | `12` | `156` | `0` | Strong 24h teammate/team-racing corpus candidate, but no live overlay sidecar. |
| `capture-20260502-155431-647` | 24h continuation | `55975` | `12` | `33` | `0` | Useful capture-finalization edge case: manifest says `frameCount=0`, but frame scan proves `55975` frames. |
| `capture-20260502-141919-875` | 24h startup fragment | `50` | `12` | `2` | `0` | Startup edge case only. |
| `capture-20260502-141936-220` | 24h startup fragment | `9` | `12` | `2` | `0` | Startup edge case only. |
| `capture-20260502-141939-725` | 24h startup fragment | `1` | `12` | `1` | `0` | Startup edge case only. |

Important boundary: these older packages are index-only. They do not include diagnostics, live overlay route counters, `performance.jsonl`, `capture-synthesis.json`, production model replay, or pixels. As a result, every overlay currently reports `route=0`, `render=0`, and `rawSignal=no`; for these captures that means "evidence missing", not "overlay had no data".

V1.1 requirement discovered by this sweep: a bare raw capture must still be useful. The tool should either auto-run/generate the compact synthesis it needs, or decode enough raw fields directly to produce per-overlay semantic evidence. Otherwise long team-racing captures are stored successfully but cannot answer teammate, spectator, standings, fuel, radar, or pit-service questions without a separate diagnostics bundle.

GR86 status: no matching raw `capture-*` folder was found in the workspace. Candidate standalone IBT files exist under `captures/IBT/toyotagr86_*.ibt`, but the forensics tool does not accept IBT input yet. Add an IBT import/replay mode or copy the spectated GR86 raw capture into the repo before this can become part of the replay corpus.

## Overlay Findings

### Garage Cover

Garage Cover did not fail to load in OBS. OBS loaded `/overlays/garage-cover` and polled it heavily:

- Race 1: `/api/overlay-model/garage-cover=26333`, `garage-cover|model-hidden=1398`, `garage-cover|model-render=0`
- Race 2: `/api/overlay-model/garage-cover=46348`, `garage-cover|model-hidden=2458`, `garage-cover|model-render=0`

The image and route were ready in both diagnostics:

- `routeEnabled=true`
- `routeStatus=listening`
- `imageStatus=ready`
- `imageFileName=cover.png`
- `imageLength=5380858`

The model stayed hidden because the product layer reported `disabled | product hidden`.

Policy clarified after the Navarra OBS retest:

- OBS route polling proves only that the browser source is connected.
- The live cover should render only when Garage Cover is enabled in TMR settings and fresh telemetry reports `IsGarageVisible=true`.
- Disconnected, waiting, stale, settings-preview, and garage-hidden states should not cover.

Important caveat: the sampled raw captures show `IsGarageVisible=false` for all sampled frames in both full races. That means these two raw captures do not prove a live garage-screen transition occurred. They do prove that the OBS source was product-hidden the entire time even though the route and image were ready.

Fix direction:

- Expose the Garage Cover visibility toggle in settings so the product-enabled state is explicit.
- Keep the settings Preview region image-only; it must not force the live OBS cover open.
- Add deterministic replay fixtures with enabled/disabled, `IsGarageVisible=true`, garage hidden, stale telemetry, disconnected telemetry, and image missing/ready states.

### Flags

Flags have the strongest local-context evidence gap. The race-long meatball in these captures was real, so duration alone must not classify a meatball as false.

Race 1 raw diagnostics:

- `framesWithDisplayFlags=105014` out of `122998`.
- `displayKindCounts.Meatball=90905`.
- Longest display was `Critical:Meatball` for `515.199` seconds.
- `0x10140000 -> Critical:Meatball:Repair` happened on `68091` frames.

Race 2 raw diagnostics:

- `framesWithDisplayFlags=49375` out of `206109`.
- `displayKindCounts.White=28325`.
- `displayKindCounts.Checkered=13906`.
- `displayKindCounts.Black=862`.
- Longest display was `Finish:White` for `281.345` seconds.

Likely risk area:

- `src/TmrOverlay.App/Overlays/Flags/FlagsOverlayViewModel.cs` maps global `SessionFlags` bits directly to driver-visible critical flags.
- `RepairFlag=0x00100000` becomes `FlagDisplayKind.Meatball`.
- `FurledFlag=0x00080000` contributes to black/furled display.
- In these captures, the race 1 meatball must be preserved as real/local-driver-supported or manually annotated real. Other long-lived global-derived critical/finish states still need local/session-context validation before the overlay treats them as driver-visible state.

OBS caveat:

- OBS never loaded Flags in either full-race bundle: `html=0`, `model=0`, `render=0`.
- So this is a raw/native/model correctness issue, not an observed OBS pixel issue from these bundles.

Fix direction:

- Revisit whether global `SessionFlags` can ever drive local-driver critical flags without driver-specific validation.
- Prefer car-index-specific flag sources for local black/meatball state if available.
- Add raw capture replay assertions for the two full races so confirmed real meatball state is retained, while black/white/checkered/furled states require local-driver or session-state evidence.

### Fuel Calculator

Fuel had good raw input, but live measured burn never became available.

Race 1:

- `framesWithFuelLevel=118350` out of `122998`.
- `FuelLevel:Reliable=118350`.
- `FuelUsePerHour` present on `101230` frames as partial instantaneous evidence.
- `rolling-local-fuel-delta:Unavailable:requires_completed_green_lap_delta=122998`.
- Post-race history found `completedValidLaps=0`, `validDistanceLaps=3.4894`, `fuelPerLapLiters=13.3843`, quality `medium`, reason `no_completed_laps`.

Race 2:

- `framesWithFuelLevel=202597` out of `206109`.
- `FuelLevel:Reliable=202597`.
- `FuelUsePerHour` present on `169597` frames as partial instantaneous evidence.
- `rolling-local-fuel-delta:Unavailable:requires_completed_green_lap_delta=206109`.
- Post-race history found `completedValidLaps=6`, `validDistanceLaps=6.3227`, `fuelPerLapLiters=13.6225`, quality `high`.

The race 2 mismatch is important: post-race history can see six completed valid laps, but the live model never considered measured green-lap burn available. The live Fuel Calculator likely used history/model fallback instead of current-session measured burn for the whole race.

Pit/refuel diagnostic mismatch:

- Race 1 final pit window: fuel `9.0776 -> 18.8604 L`, net `+9.7828 L`, requested fuel around `10 L`, `sawFuelIncrease=false`.
- Race 2 service stop: fuel `3.8224 -> 33.4513 L`, net `+29.6289 L`, requested fuel around `30 L`, `sawFuelIncrease=false`.
- `pitWindowsWithFuelIncrease=0` in both races.

Fix direction:

- Debug live measured-burn eligibility against completed green laps.
- Debug pit-window fuel-increase detection. Net fuel delta and post-race pit extraction saw fuel added, but live diagnostic event detection did not.
- Add replay assertions for measured burn becoming available in race 2 and for pit fuel increase detection in both races.

### Pit Service

Raw pit-service signals were present, but OBS did not load Pit Service.

Race 1:

- `pitServiceSignalFrames=122998`.
- `pitServiceRequestFrames=114840`.
- `PitSvFlags` sampled values include `48`, `63`, `16`, and `0`.
- Two pit windows were detected.

Race 2:

- `pitServiceSignalFrames=206109`.
- `pitServiceRequestFrames=201446`.
- `PitSvFlags` sampled values include `48`, `63`, `16`, and `0`.
- Three pit windows were detected.

OBS caveat:

- `pit-service` had `html=0`, `model=0`, `render=0` in both bundles.

Fix direction:

- Treat "raw pit service exists but OBS never requested Pit Service" as an OBS readiness/setup failure when the overlay is expected to be active.
- Share the Fuel/Pit pit-window fix above, since both depend on the same service/fuel semantics.

### Track Map

Raw Track Map data was strong.

Race 1:

- `framesWithSectors=122998`.
- `framesWithLiveTiming=114471`.
- `framesWithHighlightedSectors=1161`, all personal-best sector highlights.
- OBS had `/api/overlay-model/track-map=10778`, `model-render=268`, `model-hidden=40`.
- The retained event/model indicates `track map | circle fallback`.

Race 2:

- `framesWithSectors=206109`.
- `framesWithLiveTiming=194866`.
- `framesWithHighlightedSectors=7063`.
- Highlight types included personal-best and best-lap.
- OBS had `/api/overlay-model/track-map=10862`, `model-render=347`, `model-hidden=45`.

Likely explanation:

- Race 1 generated the user map only after capture end, so the live overlay probably used the circle fallback.
- Race 2 reused the generated map, but the bundle does not preserve an active live Track Map payload or pixel proof showing the generated racing line during the race.

Evidence weakness:

- `localhost-overlay-models.json` has last-active telemetry context, but the retained Track Map model still ends as `shouldRender=false`, `trackMap=null`, `hidden | telemetry unavailable`.
- This weakens post-race forensic analysis.

Fix direction:

- Persist sampled active Track Map render payloads, including map source (`generated-map` vs `circle-fallback`), marker count, racing-line point count, and selected geometry id.
- Add replay checks proving the first race fallback and the second race generated-map behavior.

### Gap To Leader

Raw gap data was healthy and race-only.

Race 1:

- `framesWithData=90362`.
- `nonRaceFramesWithData=0`.
- `maxClassRows=12`.
- `maxClassGapSeconds=22.407`.
- OBS had `/api/overlay-model/gap-to-leader=4501`, `model-render=167`, `model-hidden=140`.

Race 2:

- `framesWithData=173545`.
- `nonRaceFramesWithData=0`.
- `maxClassRows=12`.
- `maxClassGapSeconds=40.735`.
- OBS had `/api/overlay-model/gap-to-leader=4585`, `model-render=215`, `model-hidden=176`.

Performance issue:

- Race 1 `overlay.gap-to-leader.design_v2.paint` p95 was `25.879 ms` against a `16.667 ms` paint budget.
- Race 2 p95 was `33.528 ms` against the same budget.

Evidence weakness:

- Final snapshots are hidden after disconnect, so graph/history presentation during the live race is not retained.
- User-observed Race 2 issue: Gap To Leader was broadly useful, but not perfect around pit stops.
- Race 2 had `pitContextFrames=11243`, but the current diagnostics retain only summary counters and final hidden snapshots. They do not retain the live graph series, selected car ids, pit-state annotations, scale mode, or axis bounds during those pit windows.
- User-observed graph readability issue: `player+N`/focused-car line selection does not handle a far trailing final selected line well. If the last selected class car is minutes behind the focused car, such as P10 roughly four minutes back, the top cars become hard to read.
- Current evidence cannot prove whether that was a model-selection, scale-selection, sticky-line, or renderer issue because active graph state was not preserved. The code has multiple relevant decisions that need capture evidence: desired car ids, focus-relative eligibility, lapped/same-lap filtering, sticky visibility, stale-line state, lap reference seconds, and selected scale.

Fix direction:

- Optimize native Gap design-v2 paint.
- Persist sampled active gap rows and graph state during replay/capture.
- Add explicit Gap graph diagnostics for selected series: `carIdx`, class position, reference/leader flags, gap-to-leader seconds/laps, delta-to-reference, current pit state, desired/sticky/stale state, selection reason, filter reason, scale mode, `maxGapSeconds`, focus-relative ahead/behind bounds, lap reference seconds, and line count.
- Add replay checks for pit entry/exit windows and for far-outlier selected lines so a minutes-behind car cannot silently flatten the useful top-car range without being reported.

### Relative

Raw Relative inputs were healthy.

Race 1:

- `framesWithReferenceProgress=90362`.
- `framesWithNearbyCars=122313`.
- `framesWithOfficialLapDelta=90362`.
- `framesWithPendingRelationship=36397`.
- `maxNearbyCars=50`.
- OBS had `/api/overlay-model/relative=4573`, `model-render=353`, `model-hidden=40`.

Race 2:

- `framesWithReferenceProgress=173069`.
- `framesWithNearbyCars=205457`.
- `framesWithOfficialLapDelta=173069`.
- `framesWithPendingRelationship=85737`.
- `maxNearbyCars=45`.
- OBS had `/api/overlay-model/relative=4657`, `model-render=432`, `model-hidden=45`.

No clear Relative correctness bug was proven by these bundles.

Evidence weakness:

- The diagnostics preserve route/page counters but not row-level in-race Relative payloads.
- The large pending/lapping relationship counts are expected on Gesamtstrecke multiclass traffic, but without sampled live payloads we cannot prove every row order, lap relationship, or gap text.

Fix direction:

- Persist sampled Relative rows at lapping/pending relationship transitions.
- Add semantic replay checks around "same lap", "one lap ahead", "one lap behind", and pending relationship displays.

### Car Radar

Raw Radar data existed, but side-signal events were sparse.

Race 1:

- `framesWithData=109481`.
- `localUnavailablePitOrGarageFrames=13517`.
- `sideSignalFrames=73`.
- Side states were `clear` and `left`.
- `sideSignalWithoutPlacementFrames=3`.
- OBS had `/api/overlay-model/car-radar=10303`, `model-render=28`, `model-hidden=271`.

Race 2:

- `framesWithData=191540`.
- `localUnavailablePitOrGarageFrames=14569`.
- `sideSignalFrames=565`.
- Side states were `clear` and `right`.
- `sideSignalWithoutPlacementFrames=167`.
- `multiclassApproachFrames=221`.
- OBS had `/api/overlay-model/car-radar=10387`, `model-render=37`, `model-hidden=346`.

No definitive Radar rendering bug was proven by these bundles.

Evidence weakness:

- Side-signal frames are a tiny fraction of each race, so page render counts being low can be expected.
- No pixel proof exists for the exact blind-spot side, fade timing, labels, or placement.
- User-observed side placement issue: Radar rendered a car as alongside at `0.0`, but visually the car was far off to the side.
- Race 2 has a matching diagnostic shape at session time `255.66666`: raw `CarLeftRight=3` (`right`) and `radar.side-without-placement` at the same frame. That proves iRacing reported a right-side spotter signal while our nearby placement logic found no timed/lap-distance candidate inside the side-overlap window.
- The capture schema has `CarLeftRight`, `CarIdxLapDistPct`, `CarIdxF2Time`, `CarIdxEstTime`, `CarIdxTrackSurface`, and `CarIdxOnPitRoad`, but no obvious per-car lateral/world-position channel. So "way off to the side" is only indirectly visible through side-signal-without-placement, track-surface/pit-road flags, and absence of a placement candidate; it is not directly measurable as lateral separation in the current capture.
- User-observed settings-preview issue: opening the Car Radar settings tab forces the Radar overlay visible as a preview, and alt-tabbing back to iRacing leaves it in preview mode while that settings tab remains selected.
- Current code shape matches that observation: `OverlayManager.SelectSettingsOverlayTab(...)` sets `_radarSettingsPreviewVisible` from selected tab plus overlay enabled state, and `ApplyOverlaySettingsCore(...)` treats `settingsPreview` as live telemetry/context allowed. It does not require the settings window to be foreground/active.

Fix direction:

- Persist sampled Radar payloads and screenshots at every `CarLeftRight` transition and at multiclass approach transitions.
- Assert that left/right display direction matches raw `CarLeftRight`.
- Assert that hide/fade timing follows the product contract.
- Classify side warnings by source: `CarLeftRight` only, physically placed nearby car, or both. A side warning without a placement candidate should be visibly distinguishable or suppressed according to an explicit product rule, not rendered as if a placed car was validated.
- Diagnostics should record the best candidate car ids around every side signal, with relative seconds/meters, lap distance, track surface, pit-road state, class, and placement evidence/missing reason.
- Gate Radar settings preview on an explicitly active settings-preview context, not only on the selected tab. Losing Settings focus or returning to iRacing should end the forced preview unless the product intentionally has a pinned preview mode.
- Diagnostics should record `settingsPreview=true/false`, selected settings tab, settings foreground/active state, and the reason the Radar overlay is visible.

### Input State

Raw input data was healthy.

Race 1:

- `122998` frames, `0` dropped.
- Stable `PlayerCarIdx=CamCarIdx=17`.
- Throttle, brake, steering, gear, RPM, and speed changed during the capture.

Race 2:

- `206109` frames, `0` dropped.
- Stable `PlayerCarIdx=CamCarIdx=19`.
- Throttle, brake, steering, gear, RPM, and speed changed during the capture.

OBS behavior:

- Race 1: `/api/overlay-model/input-state=22180`, `model-render=114`, `model-hidden=205`.
- Race 2: `/api/overlay-model/input-state=22264`, `model-render=186`, `model-hidden=217`.

No definitive Input State bug was proven by these bundles.

Notes:

- `BrakeABSactive` was never true in either capture, so ABS display behavior was not exercised.
- Raw clutch values look high because iRacing reports clutch engagement; source converts to driver-control scale with `1 - engagement`.

Fix direction:

- Persist sampled input payloads and pixels at braking, throttle, steering, gear-shift, ABS, and clutch transitions.

### Standings

Raw scoring data was strong, but OBS never loaded Standings.

Race 1:

- `framesWithData=122539`.
- `SessionResults` source on `89913` frames.
- `StartingGrid` source on `32626` frames.
- `maxRows=50`.
- `maxClassGroups=3`.

Race 2:

- `framesWithData=205634`.
- `SessionResults` source on `172102` frames.
- `StartingGrid` source on `33532` frames.
- `maxRows=45`.
- `maxClassGroups=3`.

OBS caveat:

- `standings` had `html=0`, `model=0`, `render=0` in both full-race bundles.

Replay observations:

- Exported browser replay models from the two captures showed class rows/headers and reference rows, but those exports were ad hoc and not preserved as first-class diagnostics artifacts.
- Early frames can be sensitive to StartingGrid vs SessionResults source selection.
- User-observed practice issue: Standings lightly flashed in an open practice before there were any results.
- This lines up with V1.0.3's unresolved non-race Standings concerns: race-only `GAP`/`INT`/leader semantics can leak into practice/qualifying, row/status counts can drift, and source selection between live timing, starting grid, and result snapshots needs stronger capture-backed proof.
- The current full-race bundles do not prove this practice flash because OBS did not load Standings and the strongest current captures are race sessions. A targeted practice capture/replay should inspect render/hidden transitions before usable scoring/results rows exist.

Fix direction:

- Treat "raw scoring exists but OBS never requested Standings" as an OBS readiness/setup failure when Standings is expected.
- Persist sampled Standings rows and source labels through the race.
- Add replay checks for class headers, reference row selection, class position, overall position, lap/gap text, and source transitions.
- Add a practice-before-results fixture that expects a stable hidden or stable placeholder state, not a render/no-render flash.
- Consider debouncing Standings source transitions or requiring a minimum stable row/source window before first render in practice and qualifying.

### Session Weather

OBS rendered Session Weather in both full-race bundles.

- Race 1: `/api/overlay-model/session-weather=4218`, `model-render=253`, `model-hidden=40`.
- Race 2: `/api/overlay-model/session-weather=4302`, `model-render=332`, `model-hidden=45`.

No specific weather correctness bug was proven during this pass.

Evidence weakness:

- No live payload samples or screenshots are retained for weather text, units, wetness label, forecast, or session-state gating.

Fix direction:

- Persist sampled weather payloads and pixels.
- Add semantic checks for units, rain/wetness mapping, track/air temperature, wind, and session visibility.

### Stream Chat

OBS rendered Stream Chat continuously in both full-race bundles.

- Race 1: `/api/overlay-model/stream-chat=22104`, `model-render=1201`, `model-hidden=0`.
- Race 2: `/api/overlay-model/stream-chat=42197`, `model-render=2260`, `model-hidden=0`.

Performance note:

- `overlay.stream_chat.connect` was over the `50 ms` budget once at `221.581 ms`.

No chat correctness issue was proven by these telemetry bundles.

Fix direction:

- Keep chat evidence separate from iRacing replay because it is provider-driven, not capture-driven.
- Record provider state and visible message count in diagnostics.

## Performance Findings

The live capture pipeline itself looked healthy in the two race captures:

- Raw capture dropped frame count was `0` in both full races.
- Race 2 performance samples reached `lastFramesWritten=206109`.
- `lastPendingMessageCount=0`.
- Telemetry FPS near finalization was around `55`, and the raw capture tick rate was `60`.

Over-budget metrics that matter:

- `overlay.gap-to-leader.design_v2.paint`
  - Race 1: p95 `25.879 ms` vs `16.667 ms`.
  - Race 2: p95 `33.528 ms` vs `16.667 ms`.

Over-budget metrics that are probably finalization/background, not live overlay health:

- `telemetry.finalize_collection`
- `telemetry.finalize.diagnostics_bundle`
- `diagnostics.bundle.create`

V1.1 should separate live-session performance from finalization performance in the report, so finalization cost does not obscure live overlay cost.

## What V1.1 Should Build

Build a durable overlay forensic replay tool, not another one-off script.

Proposed tool shape:

- Input:
  - one explicit `capture-*` folder produced by enhanced capture
  - optional matching `dallara-*` diagnostics folder
  - optional overlay list
  - optional frame/event selection strategy
- Output:
  - `overlay-forensics.md`
  - `overlay-forensics.json`
  - per-overlay sampled model payloads
  - per-overlay screenshots when a browser/localhost renderer exists
  - semantic assertion results
  - performance timeline summary

Current implementation slice:

- `tools/analysis/overlay_forensics.py` now writes the core forensics package, selected sample plan, route/OBS coverage, performance timeline, and tool-run status.
- `tools/TmrOverlay.OverlayModelReplay` is the production model replay exporter. It reads raw capture frames, applies session-info snapshots, feeds `LiveTelemetryStore`, calls `BrowserOverlayModelFactory`, and writes per-overlay `models.jsonl` rows for sampled frames.
- `tools/browser-review/render-model-replay-screenshots.mjs` is the browser/localhost replay screenshot hook. It serves sampled production model rows back through the browser overlay shell and writes per-overlay screenshot manifests.
- On this Mac, `dotnet` is not installed, so the forensics tool records production model replay as skipped with the exact Windows/.NET command to run. That is an evidence gap, not a Python approximation.

Required capabilities:

- Keep Windows collection gated by the Support enhanced-capture toggle. Diagnostics bundle creation alone must not enable raw capture, sampled overlay model sidecars, or live screenshot evidence.
- Replay raw capture telemetry into the same normalized live models used by production overlays.
- Sample frames around important transitions:
  - session state changes
  - race lap changes
  - `CarLeftRight` changes
  - pit-service flag changes
  - fuel-level increases
  - flag display transitions
  - scoring source transitions
  - track-map source changes
  - telemetry stale/disconnect boundaries
- Preserve the exact browser/localhost model payload rendered for each sampled frame.
- Render screenshots for browser review/localhost routes from those exact payloads.
- Record semantic manifests for each screenshot:
  - visible/hidden
  - status string
  - row counts
  - key text/data labels
  - data source and evidence labels
  - geometry/source ids where relevant
- Compare expected-vs-rendered invariants:
  - flags should retain the confirmed real meatball case, and should not show other local critical states unless local-driver evidence supports them
  - fuel measured burn should become available once completed green laps are available
  - pit fuel increases should be detected when net fuel rises
  - Garage Cover should render only when the product-visible Garage Cover sees fresh telemetry with `IsGarageVisible=true`; route/image readiness, settings preview, stale telemetry, and disconnect are not render triggers
  - Track Map should report generated-map vs fallback-map source
  - OBS source coverage should be explicit for every overlay with live data
- Pull in continuous performance JSONL samples and classify:
  - live overlay paint/update costs
  - capture health
  - finalization/background bundle costs

Renderer/surface coverage:

- Browser review and localhost/OBS should be executable locally.
- Windows native cannot be fully validated on this machine, but V1.1 should still trace native C# model/render paths and record the unexecuted Windows validation gap.
- Any overlay model or geometry contract used by multiple renderers should be shared and drift-checked.

Suggested command shape:

```bash
python3 tools/analysis/overlay_forensics.py \
  --capture capture-20260522-204847-774 \
  --diagnostics dallara-p217-lmp2-gesamtstrecke-long-20260522-214620-532 \
  --output artifacts/overlay-forensics/capture-20260522-204847-774
```

## V1.1.1 Kickoff Summary

This branch starts from the post-`v1.1.0` Windows captures, the newly app-owned forensics folder, and the older 1.0.3 bundles. The main takeaway is that the new capture plumbing is useful enough to separate several problem classes, but it still does not save the active live overlay truth we need to fix every issue from one package.

What we learned:

- App-owned forensics under `%LOCALAPPDATA%\TmrOverlay\forensics\<capture-id>` is the right user experience, but the auto-created package is currently a bootstrap/index package. It needs to run or schedule the enriched analysis, or the UI needs to say clearly that the package is not the final answer file.
- OBS was open during the v1.1.0 test: diagnostics saw OBS 32.1.2 and an `obs-browser-page` process. The localhost logs still showed only `stream-chat` route traffic, so "OBS not working" is now an OBS source/readiness classification problem, not simply "OBS was closed" or "localhost was down."
- Final `localhost-overlay-models.json` snapshots are too late. They usually show disconnected/post-session states, so they cannot prove what Relative rows, Standings rows, Track Map marker sizes, Fuel row counts, or Garage Cover pixels did during the live issue window.
- Practice/open-session behavior is under-tested. Relative had nearby cars but no official lap delta, Standings had partial side evidence but no accepted scoring source, and Track Map had focus/progress evidence that should not be treated exactly like race pre-grid state.
- Focus semantics need to be explicit per overlay. Track Map should follow the focused/camera car, while local-safety overlays such as Radar may need local-driver context unless the product intentionally supports spectator/focus mode.
- Several overlay bugs are likely policy or evidence-contract bugs rather than pure renderer bugs: chrome-only rendering, OBS readiness, Garage Cover live render policy, and dynamic sizing all need shared contracts across Windows native, browser review, and localhost/OBS.

Highest-value V1.1.1 fixes:

- Add active live model samples, renderer/pixel evidence, and per-overlay OBS readiness states to the forensics package.
- Classify OBS overlays as `not-requested`, `page-loaded-no-model`, `model-hidden`, `model-rendered`, or `browser-source-error`, including source URL/client-kind counts.
- Fix Relative practice timing fallback when iRacing shows a time gap but TMR falls back to meters or no timing.
- Fix Standings practice flashing by defining an overlay-specific chrome-only/waiting policy and replaying no-results practice transitions.
- Fix Track Map focus marker selection for non-player focused cars and split practice/open-session marker visibility from race pre-grid `HasTakenGrid` behavior.
- Fix Fuel Calculator compact sizing so rendered row/section count drives recommended height unless the user or OBS has intentionally locked the size.
- Fix Fuel live measured-burn availability and Fuel/Pit refuel detection where net fuel rises but `sawFuelIncrease=false`.
- Preserve the confirmed real meatball behavior while auditing whether global `SessionFlags` can drive local-driver critical flags without local evidence.
- Fix Garage Cover OBS readiness and live render policy so route/image readiness, product-enabled state, and `IsGarageVisible` are reported separately; preserve the brand fallback asset default.
- Fix Radar settings-preview lifetime and improve Radar samples around side-signal-only and multiclass approach cases.
- Keep Gap To Leader useful around pit stops and long-tail rows, and address the native design-v2 paint over-budget evidence.

Suggested branch order:

1. Improve forensics first: active model samples, OBS readiness classification, and issue-window semantic manifests.
2. Fix strongest replay-backed product bugs: Fuel/Pit, Fuel sizing, Relative practice timing, Standings no-results behavior.
3. Fix focus/context overlays: Track Map focus/progress policy, Radar preview/context evidence, Garage Cover OBS readiness and live render policy.
4. Tighten remaining correctness/performance: Flags local/global evidence and Gap To Leader pit/scale/performance behavior.

### Forensics-First Implementation Scope

V1.1.1 should make `%LOCALAPPDATA%\TmrOverlay\forensics\<capture-id>` the first place to answer "what did each layer believe?" The package should now carry:

- `package-status.json`: starter/enrichment status, model-replay status, renderer-replay status, and next actions.
- `obs-readiness.json`: per-overlay route/page-event classification: `not-requested`, `page-loaded-no-model`, `model-polled-hidden`, `model-rendered`, `browser-source-error`, or an explicit unclassified state.
- `live-model-samples.json` plus per-overlay `live-model-summary.json`: active replay model sample counts, render counts, row/section/header ranges, compact sample rows, and overlay-specific evidence extracted from model payloads.
- Per-overlay `semantic-manifest.json`: the raw/model/renderer fields and assertions needed to prove or disprove the current issue class.
- `evidence-gaps.json`: the remaining missing proof, especially no production model replay, no renderer replay, final disconnected snapshots, or OBS present with no telemetry overlay route requests.

Additional overlay-specific evidence fields to preserve:

- Standings: accepted scoring source, source transitions, row identity, class grouping, valid-lap gate, chrome-only waiting state, and race-only `GAP`/`INT` suppression in practice/qualifying.
- Relative: timing source precedence, official-vs-estimated gap, focus/player car identity, same-lap/lap-ahead/lap-behind relationship, and the reason a row falls back to meters or `--`.
- Gap To Leader: selected series, focus/reference/leader identity, pit state, scale mode, axis bounds, max included gap, lap reference seconds, and long-tail row compression.
- Car Radar: local-vs-focus context, `CarLeftRight`, placement candidate status, track-surface/pit-road suppression, multiclass approach threshold/source, calibration source, and settings-preview active state.
- Fuel Calculator: fuel-burn trust source, completed-lap eligibility, rendered section/row count, recommended/browser/native height, size-lock reason, and unused-height ratio.
- Pit Service: `PitSvFlags`, requested fuel/tires, pit-window transitions, net fuel delta, refuel-detected state, and local-strategy suppression reason.
- Flags: raw/global flag bits, displayed flag kind, local-driver evidence, session phase, display duration, and confirmed meatball/black preservation.
- Track Map: generated-map vs fallback source, fallback reason, focus marker identity/radius, player-vs-non-player focus, `HasTakenGrid`, practice/open-session progress policy, and sector highlights.
- Input State: latest scalar controls, trace point count, graph normalization, stale/unavailable state, and update cadence.
- Session Weather: air/track temperatures, rain/wetness mapping, wind, units, stale/unavailable state, and weather source.
- Garage Cover: product enabled state, detection state, `shouldCover`, image status/source, default brand asset readiness, route/render state, and proof that route/image readiness alone does not open the cover.
- Stream Chat: provider status, visible message count, sanitized text evidence, and external-source classification.

## V1.1.1 Candidate Notes From 2026-05-23 Live Test

These are user-observed issues from a post-`v1.1.0` Windows test while waiting for the new forensics package. Treat them as reproduction targets when starting `v1.1.1`; the capture should decide which are telemetry-source bugs, model-policy bugs, renderer bugs, or OBS setup/readiness failures.

### Relative Practice Timing

Symptom:

- Relative did not show timing in practice even though iRacing's native Relative box did.

Current code-path read:

- `RelativeOverlayViewModel` already treats Practice like Race for full relative timing display, so this is probably not a deliberate practice UI suppression.
- `LiveRaceModelBuilder.BuildRelative` can infer practice rows from proximity, class-gap, or estimated timing, but seconds only appear when `RelativeSeconds`, inferred lap-distance seconds, `DeltaSecondsToFocus`, or `CarIdxEstTime + CarIdxLapDistPct` evidence survives into `LiveRelativeRow.RelativeSeconds`.
- The v1.1.1 capture should check `SessionType/SessionName`, `SessionState`, `CarIdxF2Time`, `CarIdxEstTime`, `CarIdxLapDistPct`, reference/focus car identity, current lap-time source, and whether `BuildEstimatedRelativeRows` was blocked by missing focus estimated time, missing focus lap distance, suspicious-zero timing, or session-state gating.

Product expectation:

- If iRacing shows a practice relative time gap from live telemetry, TMR Relative should show seconds too, not fall back to meters or `--`, unless the forensics package proves the iRacing UI is using a source the SDK capture does not expose.

V1.1.1 assertion to add:

- Practice replay frame with iRacing-like timing evidence should produce Relative rows with second-formatted gaps and `source: live proximity telemetry` or `source: model-v2 timing fallback`, not meter-only rows.

### Standings Flashing / Chrome-Only Rendering Policy

Symptom:

- Standings still lightly flashes before usable practice results exist.
- Product decision to hide overlays with no body data was too broad for some overlays. If a user enabled header/footer chrome, the overlay may need to render a stable chrome-only/waiting state rather than repeatedly switching hidden/rendered.

Current code-path read:

- Browser/localhost `BuildStandings` and `BuildRelative` use `ShouldRenderTable(columns, rows)`, so an empty row set can force `shouldRender=false` even when headers, time remaining, or footer/source text are enabled.
- Native `OverlayManager` uses `OverlayContentSizing.HasRenderableContent(...)` for content/settings gates, but the actual model can still return waiting rows/no rows. The cross-surface policy is not explicit enough: "no body rows" currently acts like "no renderable overlay" in some paths.
- Existing Standings practice behavior also has a valid-lap gate, scoring-source transitions, session-results snapshots, and timing fallback sources that can make the model alternate between `waiting` and renderable rows.

Product expectation to revisit:

- Define overlay-by-overlay chrome-only behavior. For Standings and possibly Relative, enabled header/footer chrome should be allowed to render a stable waiting shell when body data is not ready. Other overlays may still be true no-render when their body is unavailable.
- The fix should avoid reintroducing misleading live-looking data. Waiting chrome must have clear status/source evidence and no stale rows.

V1.1.1 assertions to add:

- Practice-before-results Standings replay should not alternate `model-hidden`/`model-render` without a real source transition.
- If the chrome-only policy is enabled for Standings, `shouldRender=true` should be allowed with `rows=[]` only when header/footer evidence is enabled and status/source clearly say waiting.
- If chrome-only policy is disabled for an overlay, no-data should remain hidden and should not emit empty shells.

### OBS Still Not Working

Symptom:

- OBS still did not behave correctly after the v1.1.0 fixes.

Current evidence boundary:

- Prior full-race bundles showed localhost itself was healthy, while some overlays had zero OBS route requests and Garage Cover was being polled but hidden. That distinguishes at least three failure classes: scene/source absent, route not requested, and route requested but model hidden.
- The new v1.1.0 forensics package should tell us whether OBS requested `/overlays/<id>`, `/api/overlay-model/<id>`, and `/api/browser-source-event`, plus whether browser-source page events report `model-render`, `model-hidden`, or errors.

V1.1.1 assertions to add:

- For every overlay expected in OBS, classify OBS state as `not-requested`, `page-loaded-no-model`, `model-hidden`, `model-rendered`, or `browser-source-error`.
- Report source URLs and client kind counts per overlay so "OBS not working" immediately separates app route behavior from OBS scene/source configuration.
- Preserve Garage Cover as a localhost-only product surface: if the source is loaded, route/image readiness is necessary but not sufficient; the production model should render only when Garage Cover is enabled and fresh telemetry reports `IsGarageVisible=true`.

### Track Map Focus And Marker Sizing

Symptoms:

- Track Map did not switch focus: it kept the LMP2 dot larger even when the user focused a GT3.
- All dots were small until cars crossed the start line, then they became normal size again.

Current code-path read:

- Track Map marker focus comes from `LiveRaceModels.Reference.FocusCarIdx`, then scoring/timing/spatial fallbacks. If the map kept the LMP2 large, either the live reference/focus model did not switch to the GT3, the marker builder failed to prefer the new focus, or render smoothing retained stale focus state.
- Marker color/size distinguishes focus and player-focus: native uses a larger marker for any focus marker, while the shared render model uses focus radius plus position label sizing. A non-player focused GT3 should still be the large focus marker, but not necessarily the player-focus accent color.
- Non-focus timing markers are gated by `TrackMapMarkerPolicy.ShouldRenderTimingMarker`: non-focus cars only render when `row.HasTakenGrid` is true. That matches the observation that many cars stayed small/missing/degraded until crossing start/finish. This policy was meant for pre-green grid semantics, but it may be wrong for practice/open-session timing where cars are already visible in iRacing.

Forensics fields needed:

- `PlayerCarIdx`, `CamCarIdx`, `FocusCarIdx`, `FocusIsPlayer`, focused driver class/car path, `CarIdxLapDistPct`, `CarIdxTrackSurface`, `HasTakenGrid`, `SessionState`, `SessionType`, Track Map marker list, render marker radius, `isFocus`, `isPlayerFocus`, label, and class color for frames before and after focus switch/start-line crossing.

V1.1.1 assertions to add:

- When focus changes from LMP2 to GT3, exactly the GT3 marker should become `isFocus=true` and use focus radius on native and localhost/browser render models.
- Non-player focus should still render as the large focus marker, with distinct color/tone if we want to reserve the player accent for the local car.
- Practice/open-session markers should not depend on `HasTakenGrid` if valid `CarIdxLapDistPct` and driver identity exist. Race pre-grid may still need a separate pending-grid rule.

### Fuel Calculator Compact Height

Symptom:

- Fuel Calculator did not shrink when only two rows were visible.

Current code-path read:

- Shared sizing already has a compact path: `OverlayContentSizing.FuelCalculatorHeightForContent(rowCount, sectionCount)` clamps row/section geometry between the minimum compact height and the full default height.
- Native/browser recommended sizing uses `OverlayContentSizing.FuelCalculatorHeight(...)`, which derives expected rows from enabled Fuel Calculator blocks for the current session.
- Browser/localhost model evidence has a second pass in `BrowserOverlayModelFactory.FuelBrowserSourceHeight(...)` that derives height from the rendered `MetricSections` row/section count when `model.ShouldRender=true`.
- That leaves several likely failure modes: the live model rendered two rows but the native form or OBS source stayed at the persisted/default size; enabled content blocks implied more rows than the live sections actually displayed; scale/custom dimensions locked the surface; or the browser CSS content height changed while the source/canvas size did not.

Forensics fields needed:

- Session kind, enabled Fuel Calculator content blocks, `model.metricSections` titles/row counts, `effectiveSettings.rendered.browserSource.baseHeight/height/scale`, `effectiveSettings.rendered.layout.unusedHeightRatio`, CSS `--fuel-content-height`, OBS source/client rect if available, Windows native form/client size, and an explicit size reason such as `settings-content`, `rendered-content`, `custom-size`, or `persisted-window`.

Product expectation:

- If exactly two Fuel Calculator rows are rendered, the recommended/browser/native height should shrink to the compact geometry for those rows unless the user has intentionally locked a custom size.
- If OBS cannot physically resize its source after page load, the page should still report that distinction so we know the model was compact but the capture/source canvas was not.

V1.1.1 assertions to add:

- Practice/non-race replay with Fuel Range and Fuel Usage only should produce two rendered sections/two rows and a compact height matching `FuelCalculatorHeightForContent(2, 2)`.
- Race replay with only Plan/Fuel rows should produce two rows in one section and a compact height matching `FuelCalculatorHeightForContent(2, 1)`.
- If rendered row count and browser/native/OBS height disagree, the evidence must identify whether the cause is custom/persisted sizing, OBS fixed source dimensions, or a model-to-renderer sizing bug.

### Minimum Scale Visual Fidelity

Symptom:

- Teammate feedback reported Standings at minimum scale / 60% did not visually scale the overlay; it effectively reduced the available surface and hid/cropped columns.

Current code-path read:

- Settings and localhost model evidence already treat scale as a browser-source size multiplier: `effectiveSettings.rendered.browserSource.width/height` are derived from `baseWidth/baseHeight * scale`.
- The table render paths still use unscaled column widths, row heights, padding, and font sizes. On Windows native, `OverlayManager` shrinks the form client size, but `DesignV2LiveOverlayForm.DrawTable` keeps `fit = 1f` and draws the original columns into the smaller client rectangle. In localhost/browser, `overlay-shell.js` prefers `browserSource.baseWidth` and fixed column widths, so the page can advertise a scaled source size while rendering the full-size table.
- This means scale is currently mixed with responsive/crop behavior for table overlays. That violates the product expectation in `docs/settings-overlay-logic.md`: compact content settings may make a table narrower, but the shared Scale control should remain a true visual scale.

Forensics fields needed:

- Overlay id, scale percent, base browser/native size, rendered browser/native size, visible columns, row count, rightmost cell bounds, text-fit evidence, screenshot dimensions, DOM/native layout bounds, OBS source/client rect, and whether any content column or row was hidden because of scale.

Product expectation:

- At minimum supported scale, each overlay should preserve its configured product content unless a separate content setting disables it.
- Scale should reduce rendered geometry proportionally: table columns, fonts, row heights, chrome, graph/canvas content, markers, and control hit targets should fit inside the scaled surface.
- Responsive column hiding is a future explicit feature, not what the Scale slider means.

V1.1.1 assertions to add:

- Add minimum-scale browser-review and localhost screenshot states for every overlay with scale support, plus Windows-native screenshot coverage where the overlay exists natively.
- Add semantic manifest assertions that the 60% variant still contains the expected primary product content: Standings keeps the rightmost enabled column, Relative keeps configured columns/rows, Fuel/Session/Pit keep visible metric rows, Gap keeps graph/metric content, Input keeps wheel/rails/graph, Radar/Track Map keep marker geometry, Flags keep all active flag icons, Stream Chat keeps chat rows/chrome, and Garage Cover keeps the cover image aspect/crop.
- Add a targeted Standings minimum-scale regression that fails if `scalePercent=60` changes the visible column set or clips the rightmost enabled cell instead of scaling the table.
- Add replay/forensics checks that report scale, source size, renderer size, and content loss together so future captures can distinguish "OBS source too small" from "renderer ignored scale".

Metric-overlay agent audit:

- `fuel-calculator` has strong normal-scale coverage across browser-review, localhost, Windows native screenshots, manifests, validators, browser tests, C# fuel/model tests, and generic V1.1 replay/forensics. True waiting/no-data is intentionally hidden/no-render, and chrome-off removes header/footer chrome.
- Fuel Calculator does **not** have durable minimum-scale screenshot evidence. The risky part is not only visual crop: native model building uses scaled height through `FuelVisibleRowsForHeight`, so 60% scale can reduce row budget and hide lower-priority product rows instead of purely scaling geometry. Treat that as a possible product bug unless row dropping at scale becomes an explicit contract.
- Fuel minimum-scale patch points: `tools/browser-review/render-screenshots.mjs`, `tools/TmrOverlay.WindowsScreenshots/Program.cs`, `tools/validate_overlay_screenshots.py`, `tests/browser-overlays/browserOverlayPlaywright.pw.spec.js`, `tests/TmrOverlay.App.Tests/Overlays/BrowserOverlayModelFactoryTests.cs`, `tests/TmrOverlay.App.Tests/Overlays/FuelCalculatorViewModelTests.cs`, and `docs/fuel-calculator-logic.md` if row-budget compaction is intentionally retained.
- `session-weather` has strong normal-scale coverage for live, chrome-off, missing-weather, section-off, and no-data states across screenshot/validator/browser/C# layers. True no-data is hidden/no-render; missing weather with session data is a visible section-aware placeholder state.
- Session / Weather does **not** have durable 60% evidence. The browser renderer uses base simple-telemetry dimensions rather than an explicit scale transform, so a 60% OBS/browser-source viewport is not proven to avoid clipping or unreadable text. No current product contract says scale may hide session/weather content.
- `pit-service` has strong normal-scale coverage for active, idle, section-off, no-data, chrome-off, tire-grid geometry, browser/C# model behavior, and generic V1.1 replay/forensics.
- Pit Service is the highest-risk metric overlay at minimum scale because the tire grid is tall and dense. There is no durable screenshot/test proving 60% keeps service rows and tire analysis visible/readable without clipping, and no product contract says scale may drop pit-service content.
- Session / Weather and Pit Service patch points: add minimum-scale variants in `tools/browser-review/render-screenshots.mjs` and Windows screenshot specs, add validator checks in `tools/validate_overlay_screenshots.py`, add Playwright readable/bounds assertions, add C# scale `0.6` model/size tests, and clarify expected scale behavior in `docs/simple-telemetry-overlays-logic.md` or `docs/overlay-logic.md`.

Table/gap-overlay agent audit:

- `standings` has populated browser-review and localhost variants for race/practice/qualifying plus chrome/content variants in `tools/browser-review/render-screenshots.mjs`; Windows native variant specs exist in `tools/TmrOverlay.WindowsScreenshots/Program.cs`; validators cover variants/manifests in `tools/validate_overlay_screenshots.py`; C# covers model/native chrome-only waiting through `BrowserOverlayModelFactoryTests.cs` and `OverlayInputTransparencyTests.cs`; replay tooling covers live table behavior through `tools/browser-review/validate-race-start-replay.mjs` and model replay.
- Standings is missing explicit chrome-on/no-data screenshot validation even though product/docs/code say a header-enabled waiting state can render with no rows. Existing `standings-no-content` proves hidden/no-enabled-content, not telemetry no-data. Standings also has no 60% evidence proving all enabled columns remain present and geometry scales.
- Standings patch points: add `standings-no-data` and `standings-min-scale` to `tools/browser-review/render-screenshots.mjs`, `tools/browser-review/server.mjs`, `tools/TmrOverlay.WindowsScreenshots/Program.cs`, and `tools/validate_overlay_screenshots.py`; add tests in `reviewServerContracts.test.js`, `standingsRendering.test.js`, `settingsEffectMatrix.test.js`, `browserOverlayPlaywright.pw.spec.js`, `OverlayContentColumnSettingsTests.cs`, and `BrowserOverlayModelFactoryTests.cs`. If 60% currently hides/crops columns, product files likely include `src/TmrOverlay.App/Overlays/BrowserSources/Assets/scripts/overlay-shell.js`, `src/TmrOverlay.App/Overlays/BrowserSources/Assets/styles/overlay.css`, and native table rendering in `DesignV2LiveOverlayForm.cs`.
- `relative` has populated browser-review/localhost variants for rightmost Pit evidence, row count, column toggles, chrome-off, and no-content; Windows variant specs exist; validators assert Relative columns, stable rows, and hidden no-content; browser tests cover render/hide behavior; C# view-model/settings tests cover relative semantics; replay validator covers table rows, centered reference, and lap relationship classes.
- Relative is missing explicit telemetry no-data hidden screenshot coverage. Existing `relative-no-content` and qualifying-hidden states do not prove "no fresh relative/focus telemetry" behavior. Docs say waiting/unavailable collapse to one placeholder row, while browser/native product paths hide when rows are empty, so the expected behavior needs clarification. Relative also has no 60% evidence proving geometry scales without changing/hiding columns.
- Relative patch points: add `relative-no-data` and `relative-min-scale` across `tools/browser-review/render-screenshots.mjs`, `tools/browser-review/server.mjs`, `tools/TmrOverlay.WindowsScreenshots/Program.cs`, and `tools/validate_overlay_screenshots.py`; add tests in `reviewServerContracts.test.js`, `relativeRendering.test.js`, `settingsEffectMatrix.test.js`, `browserOverlayPlaywright.pw.spec.js`, `RelativeOverlayViewModelTests.cs`, `BrowserOverlayModelFactoryTests.cs`, and `OverlayContentColumnSettingsTests.cs`; clarify `docs/relative-overlay-logic.md`.
- `gap-to-leader` has populated graph/chrome/content variants for browser-review, localhost, and Windows specs. `gap-to-leader-no-cars` explicitly validates hidden no-data/no-cars with `shouldRender=false`; validators assert graph geometry, series, trend labels, and no-cars emptiness; C# tests cover adapter/history/focus-scale/no-data hidden behavior; race-start replay validates graph pixels and bad graph data.
- Gap To Leader is missing 60% evidence proving graph/trend geometry scales rather than disabling graph/trend rows or clipping. Docs still say no-gap data status/source are `waiting`, while screenshot validation expects `hidden | race gap`.
- Gap patch points: add `gap-to-leader-min-scale` to `tools/browser-review/render-screenshots.mjs`, `tools/browser-review/server.mjs`, `tools/TmrOverlay.WindowsScreenshots/Program.cs`, and `tools/validate_overlay_screenshots.py`; add tests in `reviewServerContracts.test.js`, `settingsEffectMatrix.test.js`, `browserOverlayPlaywright.pw.spec.js`, `OverlayContentSizingTests.cs`, and `OverlayContentColumnSettingsTests.cs`; clarify `docs/gap-to-leader-logic.md`. If scale clips or hides content, likely product files include `overlay-shell.js`, `overlay.css`, the gap browser module/CSS, and native graph rendering in `DesignV2LiveOverlayForm.cs`.
- Bottom line from this slice: Gap To Leader already has explicit hidden no-data screenshot validation, but all three overlays lack minimum-scale proof. Standings and Relative also need explicit no-data behavior screenshots matching their intended product behavior.

## Uploaded Bundle Sweep From 2026-05-23

Local enriched outputs:

- Ran `tools/analysis/overlay_forensics.py` into `/private/tmp/tmr-v111-forensics/` for the three `v1.1.0-capture/captures/*` raw captures and the two Toyota `latest-capture` summaries.
- The auto-created app forensics packages under `v1.1.0-capture/forensics/<capture-id>/` were only bootstrap inventory packages: `overlay-forensics.md` says to run the offline tool for sampled model rows, renderer screenshots, and semantic checks. V1.1.1 should either run/enrich automatically after capture finalization or make the Settings UI explicit that the saved package is only an index.

Bundle inventory:

- `v1.1.0-capture/` is the useful 1.1.0 app-owned folder. It contains three Dallara P217 LMP2 raw captures at Gesamtstrecke Long: lone qualify `capture-20260523-031807-356` (`20,054` frames), practice `capture-20260523-032606-067` (`74,743` frames), and race `capture-20260523-034827-919` (`151,312` frames).
- `toyota-gr86-road-atlanta-20260523-030630-400` and `toyota-gr86-road-atlanta-20260523-030756-343` are 1.0.3 bundles. They each include `latest-capture/` summaries for short Road Atlanta practice captures, but no `telemetry.bin`, so the tool can report diagnostics/model/route evidence but cannot build frame-aligned replay samples.
- `porsche-911-gt3-r-992-watkins-glen-20260523-033358-632` metadata also reports app version `1.0.3.0`, despite being uploaded with the 1.1.0 batch. It has history/diagnostics metadata and live synthesis for Porsche 911 GT3 R at Watkins Glen offline testing, but no `latest-capture` raw package.

OBS / localhost evidence:

- The 1.1.0 Dallara practice and race diagnostics show localhost listening, but only `stream-chat` was requested: `/overlays/stream-chat=1`, `/api/overlay-model/stream-chat=38`, `model-render=2`. All telemetry overlays had `html=0`, `model=0`, `render=0`.
- That means the current "OBS still not working" evidence is primarily OBS/source coverage: the app had raw/session evidence for Standings, Relative, Gap, Radar, Fuel, Pit Service, Flags, Track Map, Input State, Session Weather, and Garage Cover, but OBS/localhost never requested those routes.
- Follow-up after confirming OBS was open: `window-z-order.json` captured OBS 32.1.2 (`obs64`) and an `obs-browser-page` process during the race bundle, so the issue is not simply "OBS was closed." The localhost request log still saw only a Chrome-like `stream-chat` page. V1.1.1 should distinguish OBS process present, OBS browser process present, and per-overlay TMR route heartbeat present.
- Garage Cover in the v1.1.0 race diagnostics built a disconnected model with `shouldRender=true`, `detectionState=iracing_disconnected`, and `imageStatus=ready`, while the route was never loaded. That old fail-closed policy was clarified after the Navarra OBS retest: disconnected/stale states should stay hidden, and the OBS source still needs per-overlay route heartbeat evidence.
- The Toyota 1.0.3 bundles and the Porsche 1.0.3 bundle had `localhost.totalRequests=0`, so they are not useful for OBS route coverage beyond proving the older diagnostics did not capture any OBS/browser-source traffic.

Overlay evidence from the 1.1.0 Dallara captures:

- Relative practice timing: the practice capture had nearby cars on `51,153` frames but `framesWithOfficialLapDelta=0`. Global lap-delta quality was usable on `32,859` frames, and `CarIdxEstTime` was populated on many car slots. This strengthens the hypothesis that practice Relative needs a stronger estimated-time fallback when iRacing shows timing but official relative delta is absent.
- Standings flashing: the practice capture had no accepted scoring source (`scoring.framesWithData=0`, `sourceCounts.None=74,743`) even though coverage probes saw live scoring/timing/spatial rows. This matches a partial-data transition problem: Standings can have enough side evidence to twitch but not enough accepted source data to render stable rows. The chrome-only/waiting policy needs a replay test against this practice capture.
- Radar focus/timing: the practice capture had `nonPlayerFocusFrames=34,811` and `localSuppressedNonPlayerFocusFrames=34,811`. `CamCarIdx` changed from player car `0` (Dallara) to GT3 cars `4` and `5`, while `PlayerCarIdx` stayed `0`. That is exactly the focus-vs-local ambiguity behind Radar and Track Map: local-driver overlays suppress or use local context, while focus-driven overlays need to follow `CamCarIdx`.
- Radar side signal: the race capture had `sideSignalFrames=1,638` and `sideSignalWithoutPlacementFrames=165`, but `multiclassApproachFrames=0`. The capture can help with side-signal placement ambiguity, but it does not prove the reported GTP-behind timing case without model replay or richer sampled row payloads.
- Track Map focus/marker sizing: the practice capture proves the camera focus switched from Dallara to GT3 cars (`CamCarIdx 0 -> 4 -> 5 -> 4`). Current diagnostics do not preserve Track Map marker `isFocus`, radius, label, or source per frame, so they cannot prove which dot was large. The raw timing evidence still supports the suspected `HasTakenGrid`/progress gating issue: Track Map had sectors all frames, live timing on `38,791 / 74,743` practice frames, and many invalid-progress sector samples.
- Fuel compact height: v1.1.0 settings stored Fuel Calculator at `503 x 298`. Performance samples include `503 x 298`, `503 x 315`, and some `503 x 178` states. The likely race bug is now clearer: native sizing reserves enabled Stint Targets rows, but `FuelCalculatorViewModel.BuildMetricSections` only renders stint rows when `HasTrustedStrategy` is true. In the race capture, measured burn stayed unavailable for all `151,312` frames, so two visible race rows could still keep the larger enabled-content height.
- Fuel / pit service: practice detected one fuel increase, but the race capture had a real pit window with `netFuelDeltaLiters=+3.594` while `sawFuelIncrease=false`, and `pitWindowsWithFuelIncrease=0`. This remains a concrete replay target.
- Flags: the v1.1.0 race capture did not contain the earlier meatball case. It did contain White/Checkered/Debris/Yellow/Green display evidence, with longest display `Finish:White` for about `289s`.

Tooling gaps found by this sweep:

- The app-owned package location worked, but the package is not yet the "answer file"; it indexes evidence and points the user to the offline tool.
- We still lack active sampled model rows and renderer pixels. Final `localhost-overlay-models.json` snapshots are post-session stale states, so they cannot prove live row text, focus marker radius, Fuel row count, or Standings flash behavior.
- V1.1.1 should add saved active model samples for the issue windows: Relative practice timing, Standings no-data transitions, Radar side/approach timing, Track Map focus marker changes, Fuel row-count/height, and Garage Cover route/render state.

## Initial Fix Backlog From This Deep Dive

Highest confidence fixes:

- Audit Flags global `SessionFlags` mapping so confirmed real meatball cases are preserved, while global repair/furled bits do not become local-driver critical flags without local evidence.
- Fix Garage Cover OBS behavior so a ready cover image and ready OBS route are distinguishable from live render eligibility: disabled stays hidden, enabled + fresh `IsGarageVisible=true` renders, and stale/disconnected/settings-preview states stay hidden.
- Fix Fuel live measured-burn availability for race 2 style captures where post-race history can see completed valid laps.
- Fix Fuel/Pit refuel event detection where net fuel increases but `sawFuelIncrease=false`.
- Add OBS readiness reporting for Standings, Flags, and Pit Service when raw capture data exists but OBS never requests the routes.
- Optimize `overlay.gap-to-leader.design_v2.paint`.
- Fix Radar settings-tab preview lifetime so the forced preview does not remain on top after the user returns focus to iRacing.
- Reproduce and fix practice Standings flashing before results exist; preserve V1.0.3 checks for non-race `GAP`/`INT` semantics, row identity, row/status count, and timing sanity.
- Fix Relative practice timing when iRacing exposes a time gap but TMR falls back to meters or no gap.
- Define and implement overlay-specific chrome-only waiting/no-data render policy for Standings and any other overlays with user-enabled header/footer chrome.
- Fix Track Map focus-marker selection for non-player focused cars and split practice/open-session marker visibility from race pre-grid `HasTakenGrid` behavior.
- Fix Fuel Calculator compact sizing when the rendered row/section count is smaller than the persisted/default surface, or explicitly classify the size as user/OBS locked.
- Make app-owned forensics produce or schedule the enriched semantic report automatically, not just the bootstrap inventory package.

Current branch fixes that should become V1.1 replay assertions:

- Garage Cover live render policy should be asserted against compact real-capture-derived frames in normal local/CI C# tests: disabled, enabled + garage hidden, enabled + garage visible, stale, disconnected, settings-preview, image-ready, and image-missing cases.
- Radar settings preview now requires an active Settings window, so replay/UI forensics need selected-tab plus foreground/settings-active context instead of raw telemetry alone.
- IBT sidecar selection now exposes selected source path and marks capture/source session mismatches as `succeeded_with_warnings` with `candidate_session_mismatch`; replay packages should refuse to treat mismatched sidecars as clean local-car truth.
- Track Map marker rendering should be asserted with real practice/open-session frames where official positions are absent: non-focus markers must remain readable before `CarIdxPosition`/`CarIdxClassPosition` populate, and focus marker color/position must only come from the focused car id rather than a stale timing focus row.
- Relative practice timing should be asserted with real practice/open-session frames where `CarIdxEstTime` plus `CarIdxLapDistPct` are valid even if `SessionState` is not one of the race launch/green values; practice estimated rows should not inherit the race-only session-state gate, including race-weekend practice where the broader event type is race.
- Radar multiclass timing should be asserted with frames where `CarIdxF2Time` and `CarIdxEstTime` both look plausible but disagree; the warning countdown should prefer live F2 timing and use estimated timing only as fallback.

CI direction from these fixes:

- Keep the full raw captures out of the repo and out of required CI.
- Commit compact frame/model summaries derived from real captures for policy assertions.
- Run those summaries through production C# model builders first, then through browser/localhost renderer contract tests, then through tiny forensics-package smoke tests.
- Use optional/nightly full-capture replay only as deeper evidence, not as the main branch gate.

Tier 1 now starts with `fixtures/telemetry-analysis/garage-cover-navarra-obs-policy.json`, derived from the Navarra OBS/Garage Cover enhanced capture. The fixture should stay small and policy-focused: it records the observed route/model/image counts and the minimal garage-visible, garage-hidden, stale, disconnected, preview, image-ready, image-missing, and disabled cases needed to guard the production Garage Cover model.

Tier 2 now has first-pass CI wiring in `tests/browser-overlays/forensicsFixtureContracts.test.js`, included by `npm run test:settings-effects`. It replays the compact Navarra Garage Cover fixture through browser-review model contracts and the localhost browser-source shell, then checks route ids, hidden/rendered decisions, visible image source, browser-source events, shared settings hashes, and shared geometry.

Tier 3 now has first-pass CI wiring in `tests/tools/test_overlay_forensics.py`, included by the Windows static Python unittest lane. It runs `overlay_forensics.py` against tiny packages under `fixtures/telemetry-analysis/forensics-smoke/` and validates hidden/no-visible-signal, visible/rendered, and visible/not-rendered Garage Cover classifications without committing raw capture folders or diagnostics zips.

Evidence/tooling fixes:

- Enable or explicitly configure live overlay screenshot capture in diagnostics.
- Persist sampled active in-race overlay payloads instead of only final disconnected snapshots.
- Promote ad hoc replay exports into durable diagnostics artifacts.
- Add per-overlay semantic manifests so screenshots can be validated without human guessing.
- Split live performance timeline from finalization performance in reports.

## Current Review Follow-Ups To Preserve

These are the active follow-ups from the current V1.1.1 screenshot/forensics review. Keep them visible until each has either landed in code/tests or been explicitly deferred.

- Reorganize generated screenshot artifacts into overlay-owned folders so review output is easier to scan. For example, prefer `browser-overlays/standings/race.png`, `localhost-overlays/standings/race.png`, and `settings/standings/content.png` over one large flat folder.
- Add or verify a chrome-on/no-data screenshot state for every overlay where user-enabled header/footer chrome can remain visible without telemetry rows. These should be first-class product screenshots, not incidental empty states.
- Ensure each overlay's primary product behavior is covered in three places where applicable: deterministic screenshots, compact V1.1 replay/forensics fixtures, and normal unit/browser/native tests.
- Build a non-integrated preview component for future visibility-context controls: `in pit`, `spotting`, `out of car`, and `garage`. Generate screenshots for review, but do not wire it into production settings behavior yet.
- Preserve the uploaded minimum-scale examples as evidence before deleting them: `scaled standings.png` showed 60% Standings behaving like a cropped/narrow table with missing columns and unused height, while `scaled inputs.png` showed 60% Input State with cramped rails and an effectively empty graph panel.
- Add minimum-scale screenshot/semantic coverage for each overlay with scale support. Scale should reduce visual geometry; it should not silently hide columns, rows, markers, graph content, or overlay chrome unless a separate content setting does that.
- Consume and record the remaining helper-thread audit findings for canvas/live overlays and garage/stream/OBS/replay tooling, then close the helper threads so thread capacity is not held.
- Investigate whether screenshot generation should now be replay-backed by real V1.1 capture data instead of purely synthetic fixtures. Example target: `standings-starting-grid.png` should select a compact real capture frame/window where starting-grid data exists, feed that data through the production model builder, then assert the raw grid input, normalized model rows, rendered screenshot, and semantic manifest all agree.
- Keep full raw captures out of required CI, but commit tiny capture-derived frame/model summaries for important behaviors. Use optional/nightly full-capture replay for deeper evidence.

### Remaining Audit Findings And Replay Screenshot Question

Canvas/live overlay findings to preserve:

- `track-map`: existing synthetic screenshots do not prove live map-source selection, focus marker ownership/radius, generated-map vs circle-fallback rendering, sector-highlight timing, or the practice/open-session `HasTakenGrid` policy. Replay evidence should capture raw focus/progress fields, production marker rows, rendered marker geometry, and manifest assertions for generated/fallback map state.
- `car-radar`: current evidence cannot distinguish raw `CarLeftRight` side signal, physically placed nearby-car evidence, multiclass approach timing, and settings-preview visibility. Replay evidence should sample side-signal and approach transitions, record placement candidate/missing reasons, and assert the rendered side/label/fade state from the production model.
- `input-state`: the uploaded 60% example suggests the canvas/rails can become cramped and the graph can appear effectively empty. Replay-backed screenshots should assert raw scalar inputs, normalized graph/rail values, trace point counts, rendered graph pixels/bounds, and unavailable/stale hidden behavior.
- `flags`: preserve the confirmed real meatball case while requiring local-driver/session evidence before global repair/furled/black bits become local critical displays. Screenshot/manifest coverage should prove active flag icons, inactive/no-data hidden behavior, OBS route coverage, and local-vs-global source labels.

Garage/stream/OBS/replay tooling findings to preserve:

- `garage-cover`: route/image readiness, product enabled state, fresh `IsGarageVisible=true`, stale/disconnected telemetry, settings preview, and image-ready/missing states must remain separately reported. The live cover should render only from explicit product eligibility, not because the OBS route or fallback image is ready.
- `stream-chat`: treat this as provider/external-source evidence, not raw iRacing replay truth. Manifests should record provider status, visible/sanitized message counts, route health, and any browser-source errors without trying to derive chat content from telemetry captures.
- OBS readiness: report per-overlay states such as `not-requested`, `page-loaded-no-model`, `model-polled-hidden`, `model-rendered`, and `browser-source-error`, including source URL/client kind counts. A telemetry-rich capture with zero route requests is an OBS/source coverage failure, not a model proof.
- Replay-backed screenshot investigation: decide whether important screenshots should come from real V1.1 replay/tool data rather than purely synthetic fixtures. For example, `standings-starting-grid.png` should use a compact capture-derived starting-grid frame/window, run it through production model logic, and assert `raw capture fields -> normalized model rows -> rendered screenshot -> semantic manifest`.
- CI boundary: full raw captures stay out of required CI. Commit only tiny capture-derived frame/model summaries for important behaviors, and keep full-capture replay as optional or nightly evidence.
