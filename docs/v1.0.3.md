# v1.0.3 Planning

## Deferred From v1.0.2

V1.0.2 closed the local implementation and validation work that could be done from deterministic fixtures, uploaded CI logs, and the current captures. The items below should not remain open in the V1.0.2 tracker because the next useful step is fresh diagnostic capture, real Windows/OBS validation, or a product decision that is larger than the V1.0.2 hardening branch.

### Capture And Real-Validation Items

- V102-010: prove visual layout with live overlay screenshot capture and localhost route pixels enabled. A support bundle that only has semantic overlay state should continue to say it cannot prove pixels.
- V102-011: collect a deliberate pit/setup-change capture where the driver changes wing/ARB or another setup control, then decide whether any live telemetry, IBT field, or session-info diff can support user-facing Pit Service rows.
- V102-012 and V102-044: validate track-map provenance and live replacement in a real run. The capture should prove when a map is generated during finalization, recovered on startup, selected from storage, and reloaded by the running Track Map overlay instead of staying on the fallback circle.
- V102-022 and V102-046: reproduce settings/overlay interaction issues on Windows with diagnostics enabled. Capture overlay topmost/no-activate/input-transparent transitions, settings close/exit/update-state breadcrumbs, and pixel evidence for any flash or post-update close blockage.
- V102-032: keep transient update-check failures visible in diagnostics and validate a run where failed startup/manual checks are followed by a successful final state.
- V102-035 and V102-036: collect a race or team/focus-switch capture that proves local-player fuel/strategy evidence separately from focused/reference-car progress, then decide whether any product copy or post-race summary needs further refinement.
- V102-038: collect lap-delta examples with usable nonzero values before any overlay or analysis surface treats lap-delta fields as trustworthy.
- V102-040: run a real OBS-style long session after the new budget classifications. Use performance logs to scope any timer cadence, repeated settings-apply, Gap payload, localhost polling, or memory-retention optimization.
- V102-042 and V102-048: collect flag reproductions with raw `SessionFlags` and `CarIdxSessionFlags` evidence for ordinary yellow, debris, waving yellow, random waving, caution, waving caution, and one-to-green. Debris must remain orange/yellow striped instead of plain yellow, and caution/one-to-green should stay distinct because they imply safety-car/caution-period state.
- V102-047: collect Car Radar side-transition evidence while comparing against iRacing spotter calls. The first target is side-stability correctness; richer positional precision can remain a later product improvement.

### Product Follow-Ups

- V102-003: broader overlay scale UX beyond the V1.0.2 minimum-scale clipping fix and scale-aware effective-settings evidence.
- V102-028 and V102-030: Gap To Leader focus-switch history preservation. Preserve and rebase history only if the transition can be made cleanly without graph jank; otherwise the current reset behavior remains safer.

### Release Evidence To Keep Watching

- V102-023: fastest-lap purple precedence has model/render regressions in V1.0.2, but CI/Windows screenshots still need artifact review before treating the visual bug as conclusively closed in release notes.
- V102-031: validation hardening remains a release habit. Keep adding scenario fidelity, hidden-product emptiness, text-fit, header-tone, no-content, and Gap trend evidence whenever a new branch changes overlays or settings.

## V1.0.2 Validation Branch State

Commit `7a1ac20` adds validation coverage for the V1.0.2 confirmed-fix list through `tools/validation/v102-confirmed-fix-coverage.json`, browser review tests, screenshot manifest parity metadata, Windows screenshot metadata, and screenshot validator profiles.

The branch should not be treated as proving all real product behavior yet. The first CI run reportedly passed everything except screenshot manifest parity, which means the current added checks mostly prove that coverage metadata exists and that browser/localhost/native parity evidence is emitted where the generated artifacts support it. The next V1.0.3 work should add failing validations for the capture-derived evidence gaps below, especially where the current support bundle can only show synthetic preview pixels.

## Overlay Evidence Contract Workstream

The wider testing change is tracked in `docs/overlay-evidence-contract-testing.md`.

The next durable version of this work should be the v1.1 capture replay loop described in `docs/model-v2-future-branches.md`: saved raw captures should replay into normalized live models, browser/localhost/native evidence, screenshots, manifests, and CI jobs so real-session bugs become repeatable validation fixtures.

2026-05-21 local replay-candidate review:

- Promote the GR86 Nordschleife Industriefahrten spotter-only race capture (`capture-20260520-180306-881`) first for v1.1. It is a real race-start/grid stream where the local user was deliberately spotting, not driving. That makes it the best first proof for spectator/local-role contracts: Standings, Relative, Track Map, Session / Weather, Flags, and Gap To Leader should stay focus/race-array driven, while local-only Fuel, Inputs, Pit Service, and Radar should remain hidden or degraded without synthesizing driver data.
- The GR86 capture did not expose a new named `IsSpotting`, `Spotter`, or `IsSpectating` telemetry variable, and its schema had no new fields versus the older local raw captures. The durable signal is session info: `DriverInfo.DriverCarIdx` points at a driver row whose `IsSpectator` value is true. Raw telemetry still reports `PlayerCarIdx` for that spectator row, and `CamCarIdx` usually follows other race cars. `IsReplayPlaying` is true for most frames in this spotting/watch context, so it must not be used alone as a reason to suppress live race overlays.
- Use the April VLN four-hour capture (`capture-20260426-130334-932`) as the primary long-run/local-driving stream after GR86. It has clean 60 Hz cadence, active local lap/speed/fuel/throttle/brake/steering/pit signals, multiclass team-race context, and practice/qualifying/race transitions. Its redaction and file-size cost are high, so v1.1 should commit only minimized replay slices or normalized replay windows.
- Use the May 2 24h capture `capture-20260502-143722-571` as the mid-session rejoin/no-history stream. It has good cadence and useful `CarIdx*` race data, but local fuel/input/lap scalars are mostly zero, so it should validate non-local race overlays rather than local strategy/input overlays.
- Keep `capture-20260502-155431-647` as a corrupted/truncated capture recovery case only. The binary has readable frames while the manifest reports zero frames and lacks finalization metadata. The tiny May 2 captures are disconnect/minimal-frame edge cases, not ordinary v1.1 stream fixtures.

Implementation files:

- `tools/validation/overlay-evidence-contract.mjs`: normalizes overlay models into `overlay-evidence-contract/v1` and validates semantic/data-contract rules before screenshot generation.
- `tests/browser-overlays/overlayEvidenceContract.test.js`: parallel Vitest lane for the new approach.
- `tools/browser-review/server.mjs`: browser-review model contract now emits first-class evidence fields for runtime routes, settings fingerprints, provenance, row/column identity, layout density, input availability, fuel strategy, map fallback, and unavailable-content policy.
- `src/TmrOverlay.App/Overlays/BrowserSources/BrowserOverlayModelFactory.cs`: production browser/localhost model contract now emits the same evidence shape from app models, including explicit Windows/native pixel-evidence status.
- `tests/TmrOverlay.App.Tests/Overlays/BrowserOverlayModelFactoryTests.cs`: app-side tests prove the production model factory carries the new evidence layer.
- `tools/validation/overlay-evidence-framework-comparison.json`: current-fleet vs evidence-contract comparison matrix, mapped to V103 coverage IDs.
- `.github/workflows/windows-dotnet.yml`: separate `Overlay evidence contract` CI job, so this lane can be compared against the existing fleet without merging the signals.

Command:

- `npm run test:evidence-contract`

Status:

- Keep this lane separate from `test:settings-effects` until it has proven its value against the existing fleet. The lane is now integrated into browser-review and production browser/localhost app models; missing evidence plumbing should be green, while current product/data-contract regressions remain red.
- Do not use this lane to remove screenshot validation. The intended split is semantic evidence first, screenshot/pixel/layout evidence second.
- Local check on 2026-05-19 after app integration: `npm run test:evidence-contract` runs the comparison, normalization, plumbing-separation, product-contract, explicit-provenance, and cross-contract checks. The expected failures are product families such as `chrome-contract`, `input-contract`, and `unavailable-content`; route, settings-fingerprint, provenance, table-contract, layout-density, fuel-strategy, map-fallback, and synthetic-state plumbing should no longer be the reason this lane is red.
- Local check on 2026-05-19 after app integration: `npm run test:settings-effects` remains red on product assertions. The V1.0.3 evidence-plumbing checks for shared settings fingerprints, table identity evidence, preview provenance/route/native evidence, table diagnostics, and metric layout evidence pass; remaining red assertions include redundant overlay titles, Fuel `Covered` copy, Input trace/unavailable policy, V1.0.2 non-race display contracts, and Gap trend/color evidence.
- CI log review on 2026-05-19 for `logs_69748696311`: `Overlay evidence contract` and `Settings effect matrix` are red for the intended product families, while browser review and localhost route tests pass. Unexpected CI noise was limited to evidence plumbing: Windows screenshot compile nullability, a C# snapshot assertion that accidentally required title removal, preview provenance omitted from screenshot `scenarioEvidence`, and screenshot row evidence missing class-header/placeholder identity fields. Those were validation-layer issues, not product fixes.
- Local validation after the CI-log review patches: `node --check tools/browser-review/render-screenshots.mjs`, `python3 -m py_compile tools/validate_overlay_screenshots.py tools/compare_screenshot_manifests.py`, and `git diff --check` pass. `npm run test:evidence-contract` remains red with 4 passing plumbing tests and 2 failing product tests; `npm run test:settings-effects` remains red with evidence-plumbing checks passing.
- Local screenshot validation after regenerating `artifacts/browser-review-screenshots` and `artifacts/localhost-screenshots` no longer reports the old `preview provenance missing ...` or `effectiveSettings rendered rowIdentities ...` plumbing failures. The screenshot validators still fail on product evidence: redundant overlay titles, unavailable/stale content policy, Input fixture availability/trace evidence, Fuel collapsed-height/`Covered` behavior, Track Map fallback labeling, and existing settings cursor assertions.
- CI log review on 2026-05-19 for `logs_69751809395`: Windows build and Windows tests are green, confirming the C# nullability and snapshot-title plumbing fixes. Browser/localhost screenshot validators no longer report the old missing-provenance or row-identity failures. Windows screenshot validation is still red only because the `components/settings/browser-source.png` crop was too narrow to include the native Copy control at the right edge of the Browser Source panel; widen that evidence crop so V102-001 can prove interactive cursor samples instead of reporting missing evidence.
- V102-001 settings cursor validation is now evidence-aware instead of raw CSS-string-only: passive chrome may report browser `auto` or native `default`, clickable controls may prove interactivity through `controlKind`, and move/grab cursors remain forbidden. Local browser-review screenshot validation still fails on overlay product evidence, but no longer on settings cursor noise.
- CI log review on 2026-05-19 for `logs_69755248546`: browser review tests, localhost route tests, Windows build, Windows static validation, Windows publish dry run, Windows overlay screenshots, and Windows installer screenshots are green. Browser-review screenshot generation wrote 131 screenshots; localhost screenshot generation wrote 81 screenshots. Both screenshot validators are red for product evidence, not missing metadata: redundant ordinary overlay titles across the supported overlays, Fuel Calculator waiting collapsed-height evidence, Session / Weather missing-state unavailable-content policy, Input waiting availability/trace/unavailable-content policy, and Track Map circle fallback labeled live.
- The same `logs_69755248546` run keeps `npm run test:evidence-contract` red in the intended lane shape: 4 plumbing/comparison tests pass and 2 semantic product tests fail. The failing product families are concentrated around `chrome-contract`, `unavailable-content`, and `input-contract`. The expanded failures are useful because they enumerate the ordinary overlays still carrying redundant title chrome instead of stopping at one representative overlay.
- The `logs_69755248546` Settings effect matrix is red with 2 failed files, 6 failed test cases, and 36 passing tests. The failing assertions still match the V1.0.3 product backlog: non-race Standings exposes race `GAP`/`INT`/`Leader`, non-race Relative keeps a generic `Delta` header, Session / Weather and Pit Service show race lap context outside race sessions, Gap To Leader lacks completed 5L/10L readiness and per-series color evidence, Fuel Calculator renders `Covered` from unavailable/null need, and Input State lacks valid trace/availability evidence.
- The one unexpected `logs_69755248546` failure was in the Windows native test job: `DiagnosticsBundleServiceTests.CreateBundle_IncludesTriageFilesAndExcludesRawTelemetry` expected `flagsModel.rawFlagsHex` to be `0x00000028`, but received `--`. That is validation-fixture freshness noise, not a product fix target: the test-created connected snapshot aged past the 1.5 second fresh-telemetry window while the diagnostics bundle was being assembled on CI. Keep the product failures red, but keep this synthetic diagnostics test from going red because its own timestamp expired.

## Product Fix Order

The current V1.0.3 branch is intentionally in the evidence/test-hardening phase. Product/runtime code should stay unchanged until the red validation families below have served as proof of the regressions. Once product fixing starts, work through the failures in this order so broad contract defects are removed before narrower overlay-specific bugs are debugged.

## Shared Geometry Contract Follow-Up - 2026-05-21

The table width/height parity work exposed a broader renderer-contract problem: browser review, localhost, native WinForms, screenshot generators, and validators still repeat design geometry in several places. Gap To Leader graph metrics and Stream Chat rows were the first active investigation target; the latest CI note below updates which areas are still red. The follow-up work below should stay in V1.0.3 so the investigation is not lost after the immediate CI failures are addressed.

Application/settings findings:

- Fixed in the shared-contract pass: settings shell size, titlebar/body/content bounds, sidebar and close-button geometry, panel sizes, component crop bounds, region-tab sizes, matrix row/column geometry, basic control row widths, and settings control hit areas now come from `overlay-geometry.json`, generated C# constants, injected browser CSS variables, screenshot evidence, and validator checks.
- Settings segmented controls are contract-owned because their child choice bounds and text fit are measured evidence. The full screenshot sweep found the browser was using a real CSS border that stole 2px from the native choice height; the browser should paint that border without shrinking the shared 30px/32px controls, and native/browser evidence should consume the same segmented inset and choice-gap constants.
- Still implicit or intentionally separate: preferred tab/order metadata, route/comparator aliases such as `support` / `error-logging`, and installer review geometry. Installer review remains a browser mock while Windows installer evidence comes from real `msiexec` windows, so do not force those real MSI control bounds into the app/settings contract.

Other-overlay findings:

- Fixed or partially fixed in the shared-contract pass: table configured column widths and row/header heights, overlay default sizes, metric row heights/gaps/value geometry, Pit Service metric divider/border math, Gap graph geometry, and Stream Chat row/header/badge geometry now live in `overlay-geometry.json` and generated constants instead of renderer-local literals.
- Remaining metric work is no longer "add a metric contract"; it is to keep specialized overlay body sizing, collapsed heights, metric table body bounds, and evidence row/section bounds consuming that contract everywhere `OverlayContentSizing`, browser CSS/JS, native rendering, screenshot tooling, and validators touch the same measured geometry.
- Remaining table work is to broaden the contract beyond the current width/height pass when CI starts asserting chrome/body padding, class-header geometry, cell padding, and hidden-text diagnostics from configured widths.
- Input State now has a first shared geometry section for refresh cadence, trace budget, full/graph/rail widths, content padding, graph/rail gap, rail dimensions, wheel dimensions, and compact thresholds. Remaining future work is stricter manifest evidence for exact graph/rail/body bounds and text fit once CI starts comparing those fields semantically.
- Flags now has a first shared geometry section for refresh cadence, min/max size, padding/gap, grid rules, compact thresholds, pole/cloth math, label bounds, checkered grid, and stripe/path fractions. Remaining future work is stricter rendered evidence for path/stroke details if CI needs to compare vector internals.
- Track Map and Car Radar now have shared canvas/default size fields, but still need explicit fit/aspect/source-coordinate policy evidence. Their render models already own much of the geometry, so this is lower risk than table/metric/Input/Flags parity.
- Garage Cover is browser/localhost-only today. Its default 16:9 dimensions, browser-only status, and cover-image flags are now represented in the canvas overlay contract; future evidence should report computed `object-fit: cover` crop behavior so native parity validation does not treat it as a missing WinForms surface.

Shared geometry contract pass:

- `overlay-geometry.json` now owns Gap graph geometry, metric row geometry, Stream Chat row/header/badge geometry, Input State layout geometry, Flags vector geometry, canvas/browser-only overlay sizes, settings shell/component geometry, overlay default sizes, and Standings/Relative table widths. C# constants are generated into `OverlayGeometryContractValues.g.cs`, browser/localhost CSS receives variables from the same JSON, and native renderers, screenshot evidence, and static validators read those generated values instead of mirrored literals.
- Contract-owned now: dimensions CI compares or native must reproduce, including shell/window sizes, content/body/sidebar bounds, panel sizes, region tab geometry, row/control widths, table/grid column sizes, overlay base sizes, metric row heights/gaps, graph geometry, Stream Chat row/badge/header geometry, fitting-sensitive font sizes/line heights, and button/control/check hit areas that affect evidence.
- Can stay local for now: decorative colors, gradients, shadows, border opacity, rounded-corner paint, cursor/hover styling, and purely visual font weight or padding only when it does not affect bounds, fitting, crops, or semantic evidence. If a decorative-looking value is measured by browser/localhost/native manifests or affects cropped screenshot bounds, it is contract-owned.
- Diagnostics bundles now include `metadata/overlay-geometry-contract.json` with the typed runtime geometry contract, parsed source JSON, and SHA-256 hashes. A real Windows support bundle can be compared against CI artifacts to tell whether a drift came from runtime settings, stale generated constants, source JSON, or renderer behavior.

Gap To Leader and Stream Chat waiting-for-CI prep:

- Gap To Leader needs a real geometry-contract patch rather than comparator-only normalization if the `718f3f2` CI still reports graph-off metric-table deltas. Source and panel sizes are still split between `OverlayContentSizing` (`360`, `444`, `654` base widths) and browser panel CSS/JS (`326`, `410`, `620` panel widths at `275` height). The next contract should own the full chain: browser/native source size, body bounds, graph panel bounds, graph frame, metrics table, row paint bounds, row text bounds, and metric column bounds.
- Gap metric rows mostly consume shared `gapGraph` constants today, but evidence records text bounds while the renderer paints larger cell backgrounds. Future validators should compare semantic row order and text-fit strictly, but distinguish `rowTextBounds` from `rowPaintBounds` so CI can explain whether a remaining delta is text layout, cell paint geometry, or source/body sizing.
- Stream Chat already has many shared constants in `overlay-geometry.json`, but visible-row selection is still implementation-specific. Browser review budgets rows from `rowBudget`, renders DOM, then prunes overflow; native measures GDI row heights from the newest row upward until the available body height is full. A durable fix should make visible-row selection, row measurement inputs, and row pruning policy explicit contract fields.
- Stream Chat screenshot evidence should report the rows that actually rendered, not simply `model.rows.slice(-maxRows)` paired by index with DOM bounds. If browser pruning drops older rows, the current evidence can associate a model row with the wrong rendered bounds, while native evidence already reports only rows selected by native layout.
- After CI confirms the remaining deltas, patch Gap first if graph-off metric-table geometry remains red; patch Stream Chat next if row count/bounds remain red after evidence rows are aligned to actual rendered rows.

Latest CI artifact review on 2026-05-21 for `logs_70198179567`, `browser-localhost-screenshots`, and `windows-screenshots`:

- Windows tests, overlay evidence contract, browser review tests, localhost route tests, settings effect matrix, browser screenshot validation, localhost screenshot validation, combined browser/localhost screenshot validation, Windows screenshot validation, Windows build, Windows static validation, and Windows publish dry run are green.
- Screenshot manifest parity is the remaining red lane. Gap To Leader graph-off and Stream Chat overlay rows no longer appear in the parity failure list. Gap graph-off still has small frame/table offsets in the manifests, but they are within the current tolerance and the semantic active-threat fields now match.
- Remaining browser-vs-Windows overlay parity failures are concentrated in Pit Service metric geometry: one-segment metric widths are `363` browser vs `355` native, and the tire-analysis-off section/segment widths are `420`/`287` browser vs `413`/`279` native. Treat this as metric-row geometry contract work, not a comparator-only fix.
- Local Pit Service metric-geometry fix: the shared metric contract now includes row border and value-divider widths; native metric layout/draw uses the same value rect helper as evidence, segmented rows use `valueSegmentGap` as their value-cell padding, and metric section evidence reports the title/container bounds instead of inner title text bounds. The comparator waiver for metric section title bounds was removed so CI can verify the contract directly.
- `session-weather-missing` had an effective-settings hash mismatch because Windows native evidence skipped the fixture-specific settings path and reported default weather blocks as enabled. The fix is to let that native fixture use `NativeVariantSettings`, matching the browser/localhost weather-off effective settings hash.
- Local settings/UI evidence fix: native screenshot evidence no longer emits a duplicate `settings-field-row` for `general.preview.session-data`, native region-tab evidence uses the same shell width as `DesignV2SettingsSurface`, native `support.bundle.latest` evidence was moved back to the actually drawn row, and browser support analysis state text now matches the rendered title-case `On`/`Off` text. Remaining settings failures should be treated as layout-contract work unless a later artifact proves another stale evidence path.
- Settings page/component parity now dominates the red output. The biggest buckets are support/diagnostics, general/update pages, overlay control panels, chat inputs, browser-source crops, unit choice, and region tabs, matching the planned settings geometry contract work.
- Installer parity was also red because the browser review mock and real MSI windows have different text/copy and control geometry. Keep that separate from product overlay parity: cross-source installer CI should prove menu coverage, menu identity, screenshot frame size, and required evidence, while real MSI UI geometry stays validated by the Windows installer evidence itself.

### 1. Overlay Title And Chrome Contract

Fix this first because redundant overlay chrome affects almost every ordinary overlay and pollutes browser, localhost, and native screenshot comparisons. If this remains broken, lower-level visual diffs are harder to trust because the screenshots are already carrying an obvious cross-surface contract violation.

Current evidence:

- `chrome-contract` remains one of the expected red product families in `npm run test:evidence-contract`.
- `test:settings-effects` still fails on redundant overlay titles.
- Browser/localhost/native manifests still expose ordinary overlay display titles as rendered model text where the overlay body should stand on its own.
- Localhost overlays were specifically observed to keep overlay titles even after other surfaces moved closer to the intended contract.

Fix scope:

- Remove redundant title/chrome rendering for ordinary product overlays across browser review, localhost, and native paths.
- Preserve legitimate product chrome where it is part of a surface contract, such as settings panel labels, browser-source route labels, explicit modal/window titles, and any future overlay that intentionally owns a header.
- Keep route metadata and manifest `overlayName`/identity fields. The bug is visible user chrome, not internal evidence identity.
- Prove parity through screenshot manifests rather than a single renderer-only assertion; localhost must not be allowed to keep title chrome after browser/native are fixed.

Expected validation movement:

- `chrome-contract` should turn green in the evidence-contract lane.
- Browser-review and localhost screenshot validation should stop failing on redundant overlay titles.
- Any remaining screenshot failures after this step should be easier to classify as data, layout, or availability bugs instead of generic chrome noise.

### 2. Unavailable And Stale Content Policy

Fix this second because it is the largest trust issue in the current data contract: unavailable overlays must not look live, successful, or measured unless the model explicitly declares stale-history provenance and the UI labels it accordingly. This should be treated as a shared overlay policy, not one-off cleanup for each screenshot.

Current evidence:

- `unavailable-content` remains an expected red product family in `npm run test:evidence-contract`.
- Gap To Leader can report `waiting for fresh telemetry` / `Unavailable` while rendering graph series, points, leader scale, positions, and trend rows.
- Fuel Calculator can report unavailable strategy inputs while rendering success-style copy.
- Session / Weather has no current live weather model in the latest bundle, yet preview pixels can still look authoritative because fixture provenance is not prominent enough.
- Input State, Stream Chat, Garage Cover, and some waiting/degraded states need the same policy so blank, stale, unavailable, synthetic, and live states are visually and semantically distinct.

Fix scope:

- Define a single unavailable-content contract in the model evidence: either suppress renderable content, mark it as section-aware placeholders for known/unknown fields, mark it as explicit stale-history fallback, or mark it as synthetic preview fixture content.
- Ensure waiting/unavailable renderers do not display live-looking charts, tables, metrics, or success labels from stale or null data.
- Keep deterministic fixture previews useful, but require them to say they are synthetic fixture evidence when they are not capture-backed or live.
- Make the policy apply to all three surfaces: browser review, localhost routes, and native screenshot evidence.

Expected validation movement:

- `unavailable-content` should turn green in the evidence-contract lane.
- Screenshot validators should stop reporting mixed unavailable/live-looking states.
- Future issues of the same class should fail by policy even when the overlay-specific fields change.

### 3. Input State Preview Availability And Trace Contract

Fix this third because it is a narrow, high-signal data-contract failure: the preview fixture contains valid controls, but the rendered model declares unavailable input state and no trace. This can be fixed and validated without needing a broader telemetry capture.

Current evidence:

- `input-contract` remains one of the expected red product families.
- Input State previews render `waiting for fresh telemetry`, `evidence: Unavailable`, `inputs.isAvailable: false`, and `tracePointCount: 0`.
- The same preview fixture supplies speed, gear, rpm, throttle, brake, clutch, steering, and ABS state.
- Rendered readouts show placeholder values such as `THR --`, `GEAR --`, and `SPD --` despite fixture inputs being present.
- The loose `temamate-inputs.png` shows native Inputs pixels with blank trace bands, but it is not manifest-backed and cannot prove parity by itself.

Fix scope:

- Correct the fixture-to-model mapping so deterministic preview inputs become available input evidence.
- Render trace/history evidence when fixture inputs are valid, and expose trace point counts in the manifest.
- Keep true unavailable behavior for disconnected or stale telemetry; the fix should not make every waiting state look valid.
- Add manifest-backed native/browser/localhost evidence before using loose screenshots as proof.

Expected validation movement:

- `input-contract` should turn green.
- Input screenshot validation should prove non-placeholder readouts and trace evidence for valid fixture states.
- A separate unavailable-input scenario should remain covered so future fixes do not hide real disconnected telemetry.

### 4. Fuel Calculator Strategy Copy And Collapsed Layout

Fix this fourth because it combines a misleading data message with a layout shell problem. The data message is more important than the visual blank space: `Need Covered` / `Covered` must not appear when additional fuel need, burn, stints, stops, or tank strategy are unavailable.

Current evidence:

- Fuel Calculator race preview says `Need: Covered` and `Save: None` while burn, tank, stints, stops, and additional fuel need are unavailable.
- Null additional fuel need appears to be rendered as success copy.
- The race preview uses a large 503x315 shell while only two body rows render, leaving an oversized blank lower body.
- The latest real capture is not good live fuel validation because the local car state is effectively unavailable: spectator/focus mismatch, mostly non-local focus, only limited fuel-level frames, and zero burn frames.

Fix scope:

- Treat null/unknown fuel need as unavailable or insufficient-data, not as covered.
- Separate strategy success states from unavailable states in both visible copy and evidence fields.
- Collapse or resize the body when strategy rows are unavailable so empty space does not imply missing rendered content.
- Preserve deterministic preview scenarios for both valid strategy and unavailable local-player strategy.

Expected validation movement:

- Fuel strategy/unavailable assertions should turn green.
- Collapsed-content/layout-density screenshot checks should stop failing for Fuel Calculator.
- Real-capture evidence should still be allowed to say Fuel is unavailable when the local-player data is not present.

### 5. Track Map Fallback Labeling And Provenance

Fix this fifth because Track Map currently risks presenting fallback geometry as a real live map. The user-facing harm is false confidence: a circle fallback can look like product functionality unless provenance and labels are explicit.

Current evidence:

- Track Map previews are labeled `status: live` and `evidence: Live` while manifest vector evidence says `mapKind: circle`.
- Latest capture metadata reports a current Nurburgring/Gesamtstrecke Long track key with `hasReadableMap: false` and fallback reason `no_matching_runtime_map`.
- Track-map generation diagnostics reference skipped or existing map work that appears tied to a mismatched Porsche/Spa IBT sidecar rather than the BMW/Nurburgring capture.
- The raw capture has sector and timing fields, but no current readable map document or live screenshot proof.

Fix scope:

- Label circle fallback as fallback/unavailable/synthetic, never as a live readable map.
- Expose map kind, source, selected track key, generation status, and sidecar provenance in manifest evidence.
- Validate that generated or reused map artifacts match the current capture track/car/session context before treating them as capture evidence.
- Keep fallback rendering useful for layout testing, but make it impossible for fallback screenshots to pass as live map proof.

Expected validation movement:

- Map fallback evidence should turn green.
- Screenshot validators should stop failing on circle-fallback-as-live labeling.
- Future track-map regressions should be caught by provenance checks before visual inspection.

Implementation note:

- Browser review, localhost, and the Windows screenshot generator now keep drawing the circle fallback with live dots, but report `status: track map | circle fallback`, source evidence that separates live position telemetry from missing generated map geometry, and `mapFallback.reason: no-generated-track-map`.
- Track Map markers now distinguish the local-player focused dot from an explicitly spectated non-player focus. Player focus keeps cyan; spectated focus keeps class color and timing/scoring position evidence.

### 6. Older V1.0.2 Product Contracts

After the broad V1.0.3 evidence failures above are fixed, return to the older V1.0.2 confirmed-fix contracts. These are still important, but many are easier to reason about after title chrome, unavailable policy, and shared settings parity are clean.

Current evidence:

- `test:settings-effects` still reports V1.0.2 product failures after the evidence plumbing fixes.
- Non-race Standings and Relative still need strong proof that race-only `GAP`, `INT`, leader, and timing labels do not leak into practice/qualifying views.
- Standings still has row/status mismatch evidence: previews can report `P24 | 5/5 shown` while visible rows and class headers do not match that count.
- Standings race evidence includes an impossible-looking interval such as `INT +29498.5` alongside `GAP +243.6`, suggesting incompatible row sources or missing sanity checks.
- Gap To Leader still needs completed reference-lap readiness behavior for 5L/10L, color evidence for each series, trend/threat evidence, and focus-window handling.
- Session / Weather, Pit Service, Flags, Radar, Stream Chat, and Garage Cover still have fixture-vs-capture or missing-surface evidence gaps that should not be hidden by synthetic previews.
- The latest cross-surface parity pass exposed additional browser/localhost/native evidence differences that are not visible in the normal CI lanes while Track Map fallback stops screenshot validation early: native no-data manifests can still report waiting header/text evidence after `shouldRender=false`, and shared header item tone semantics differ between web and native for otherwise matching overlays.

Fix scope:

- Re-run the V1.0.2 confirmed-fix list one family at a time and require each fix to move a named validation family from red to green.
- For Standings/Relative, prove row identity, placeholder identity, column keys, effective row settings, and timing semantics across browser review, localhost, and native evidence.
- For Gap To Leader, prove readiness, reference-lap windowing, color/trend semantics, and focus handling with data-contract evidence before trusting pixels.
- For Session / Weather, Pit Service, Flags, Radar, Stream Chat, and Garage Cover, separate real capture evidence from deterministic preview fixtures so release notes do not overclaim.
- For no-data states, make `shouldRender=false` mean no renderable header/body evidence across browser review, localhost, and native manifests; this includes Session / Weather and Pit Service no-data variants.
- Define one shared header-tone policy across browser review, localhost, and native, so clock/header tone describes clock health rather than renderer provenance or unrelated body state.
- Keep screenshot artifact upload and parity reporting available even when an earlier per-surface validator fails, so Track Map fallback or another known red item cannot hide downstream browser/localhost/native parity gaps.
- Keep performance/runtime findings as validation targets too: Gap paint budget, localhost polling volume, repeated settings apply, Stream Chat connect classification, finalization cost, memory footprint, and stopped-cleanly/update-state diagnostics.

Expected validation movement:

- `test:settings-effects` should shrink by named V1.0.2 product family rather than by broad assertion rewrites.
- Browser-review, localhost, and native screenshot validators should retain parity coverage while the failing reasons become product-specific and smaller.
- Screenshot manifest parity should stop reporting native no-data/header-tone differences once known product red items are resolved or isolated.
- The V1.0.3 doc should remain the source of truth for any V1.0.2 item that is still deferred because it requires a new real capture, OBS/Windows validation, or an explicit product decision.

### 7. Settings UI Empty Header/Footer Tabs

Hide settings UI Header/Footer region tabs when the selected overlay does not currently have Header or Footer content. Header and Footer tabs should not appear just to show an empty or placeholder panel.

Current evidence:

- Some overlays or overlay/settings states currently have no Header or Footer content to show.
- Empty Header/Footer settings tabs make it look like there are controls missing or not loaded.
- The settings UI should expose region tabs based on actual current region content, not a generic overlay template.

Fix scope:

- Hide Header and Footer region tabs when that region currently has no overlay content.
- Keep Header/Footer tabs visible when they do have content, and keep content/general tabs governed by their own product-specific controls.
- Prove browser review and native settings screenshots do not show empty Header/Footer tabs for overlays where those regions do not apply.
- Ensure screenshot validators distinguish "region intentionally unavailable" from "region missing by accident."

Expected validation movement:

- Settings screenshots should stop requiring empty Header/Footer panels for overlays that have no content there.
- Settings region metadata should explain which regions exist for each overlay, so future overlays cannot accidentally expose empty tabs.

## Latest Capture Forensics - 2026-05-19

Primary artifacts reviewed:

- `capture-20260519-144859-079/`: raw BMW M4 GT3 EVO, Gesamtstrecke Long capture with 194032 frames, 0 dropped frames, 60 Hz cadence, 161 session snapshots, and a 1.4 GB telemetry stream.
- `bmw-m4-gt3-evo-gesamtstrecke-long-20260519-154320-461/`: V1.0.2 diagnostics bundle for the latest capture.
- `temamate-inputs.png`: loose native Inputs screenshot showing an on-track/pit-lane view with the Inputs panel visible, blank trace bands, an `IN PIT LANE` game banner, and an ABS widget. It is useful pixel evidence, but it is not tied to a manifest entry or capture timestamp.

### Evidence Boundaries

Validation coverage surfaces tracked for this branch: Live overlay screenshot evidence; Preview screenshot provenance; Localhost/browser overlay chrome; Shared settings parity; Standings/Relative row contracts; Standings; Fuel Calculator / Pit Service layout; Input State; Gap To Leader; Track Map; IBT sidecar selection; Localhost route evidence; Garage Cover; Diagnostics sample frames; Flags / Radar / Stream Chat provenance.

- Live overlay screenshots were disabled in the bundle: `live-overlays/manifest.json` has `captureScreenshotsEnabled: false`, `visibleOverlayCount: 1`, and `currentScreenshotOverlayCount: 0`. `metadata/evidence-quality.json` repeats that visible overlays lack current pixel evidence.
- The preview PNGs are deterministic native preview renders using `SessionPreviewTelemetryFixtures`. They are layout evidence, not capture-specific proof. They do not represent the BMW/Nurburgring capture contents, driver roster, local-player state, or live OBS route behavior.
- Current live telemetry was disconnected when the support bundle was captured. Several desired overlays have last-active semantic state, but no current screenshot path.
- `relative`, `session-weather`, `standings`, and `track-map` were enabled, desired visible, session allowed, and context available, but `actualVisible: false`, `opacity: 0`, and `screenshotPath: null`. Only `stream-chat` was actual-visible, and it also has no pixel screenshot.
- `metadata/localhost-overlays.json` only proves localhost request stats, with the last observed path `/api/overlay-model/standings`. It does not prove per-overlay localhost route rendering or screenshots.
- `metadata/browser-overlays.json` lists `garage-cover`, but `live-overlays/manifest.json`, `live-overlays/previews/manifest.json`, and the preview PNG set do not include Garage Cover. That is a manifest/evidence coverage gap.
- `metadata/window-z-order.json` captured Windows Explorer Task Switching as the foreground full-screen surface, so z-order evidence is weak for normal driving/OBS desktop state.
- The raw capture contains Race and Warmup frames only. There is no true captured Qualifying session evidence; qualifying preview PNGs are synthetic fixtures.
- `capture-20260519-144859-079/ibt-analysis/status.json` says IBT analysis succeeded using `porsche9922cup_spa 2024 up 2026-05-18 15-17-24.ibt`, from a different car, track, and day. The latest capture is BMW M4 GT3 EVO at Gesamtstrecke Long on 2026-05-19. IBT sidecar evidence is therefore mismatched and should not support conclusions for Gap, Fuel, Pit, Weather, Track Map, Radar, or local-car behavior.
- `metadata/ibt-analysis.json` hides the selected IBT `sourcePath` in the latest-capture summary even though `ibt-analysis/status.json` exposes it. That weakens forensic triage.
- `live-overlay-diagnostics.json` aggregate counters are useful, but the 240 sampled frames are all Warmup while the capture is mostly Race. The option name says `maxSampleFramesPerSession`, but the current implementation behaves like a global cap, so race-phase row examples are missing.

### Overlay Findings

#### Standings

- Product bug: all three Standings previews say `P24 | 5/5 shown`, but the rendered manifest contains 4 car rows plus 2 class headers. The fixture includes `#60 Tommie Wittens`, but the row is clipped by the 313 px native render. Status and visible rows are out of contract.
- Product bug: `standings-race.png` shows `#3094 Tech Mates Racing` with `INT +29498.5` while `GAP` is `+243.6`. This looks like incompatible scoring rows and F2 timing rows being merged without sanity checks.
- Evidence gap: the preview drivers/classes are synthetic LMP2/GT3 fixture data, not the single race-class BMW capture. These PNGs cannot prove live capture ordering, local-player behavior, or OBS/browser parity.
- Existing V1.0.2 concern remains worth validating: practice/qualifying standings still need capture-backed proof that non-race `GAP`/`INT` semantics are correct and clearly labeled.

#### Relative

- Evidence gap / contract clarity issue: practice and qualifying previews render 10 stable rows with only 4 filled; race renders 10 rows with 5 filled. Status counts cars, not placeholder rows. Validators need to distinguish padding rows from missing cars.
- Product/UX concern: non-race relative deltas display meter values such as `-4568m`, `-863m`, and `+457m` under a generic `Delta` header. Race deltas appear to be seconds but are unlabeled. This needs semantic validation across browser, localhost, and native.
- Evidence gap: Relative previews are also fixture-only and cannot prove the BMW capture's relative rows.

#### Fuel Calculator

- Capture-data limitation: the latest live capture is not a good Fuel Calculator validation source. `DriverCarIdx: 63` is a spectator entry, focus is usually another car, diagnostics show `focus_on_another_car=194001`, `not_in_car=31`, only 459 fuel-level frames, and zero burn frames. Live Fuel being hidden/unavailable is expected here.
- Product bug: `fuel-calculator-race.png` says `Need: Covered` and `Save: None` while burn, tank, stints, stops, and additional fuel need are unavailable. Null additional fuel need appears to be rendered as success copy.
- Product/layout bug: the race preview is 503x315 but only draws two body rows, leaving a large blank lower body. The native size follows the race/default content shell even when renderable strategy rows collapse.
- Data sanity concern: the race preview shows current fuel around `104.9 L` even though the BMW M4 GT3 EVO capture reports a 100 L max tank. Because the preview is synthetic, this is fixture/model evidence rather than capture evidence.

#### Pit Service

- Capture-data limitation: the latest live capture has no real pit-service request. `PitSvFlags=0`, `PitSvFuel=0`, `PlayerCarPitSvStatus=0`, `PitstopActive=false`, `PlayerCarInPitStall=false`, `OnPitRoad=false`, `pitServiceRequestFrames=0`, and `pitServiceLocalStrategyUnavailableFrames=194032`.
- Evidence gap: `pit-service-race.png` shows hold, red service active, refuel, tearoff, fast repair, and tire changes, but that is fixture-only and contradicts the real capture counters.
- Product/layout concern: Pit Service previews are 530x707 after the tire-grid header compaction, and still need capture-backed proof that the fixture-sized body is appropriate outside the synthetic service-active state.
- Existing V1.0.2 wing/setup-change item remains unproven. This capture does not contain a local setup adjustment signal.

#### Session / Weather

- Evidence gap: preview content is not from the BMW capture. `session-weather-race.png` shows Aston Martin Vantage GT3 EVO, Gesamtstrecke 24h, team mode, and fixture weather, while the capture is BMW M4 GT3 EVO, Gesamtstrecke Long, `teamRacing: false`.
- Evidence gap: the raw capture has weather fields, but the bundle cannot prove live Session/Weather pixels because current telemetry is disconnected, `weatherModel` is null/unavailable, no last-active weather model is preserved, and live screenshots are disabled.
- Evidence gap / fixture clarity: preview wetness displays `Unknown` and declared state `Dry`. That matches fixture values, but the real capture is effectively dry after initial samples. The manifest should make fixture-vs-capture weather source explicit.

#### Gap To Leader

- Not a raw-data absence: the latest raw capture has usable Gap inputs. Diagnostics show `gap.framesWithData: 164950`, `maxClassRows: 46`, `maxClassGapSeconds: 372.412201`, and source counts from `CarIdxF2Time` plus inferred `CarIdxLapDistPct`.
- Evidence gap: the support bundle cannot prove live Gap pixels. Gap was enabled but not desired/session-allowed at bundle time, `actualVisible: false`, `liveTelemetryAvailable: false`, and `screenshotPath: null`.
- Evidence gap / product confusion: `gap-to-leader-race.png` is marked `status: waiting for fresh telemetry` and `evidence: Unavailable`, but still renders a graph with 3 series, 6 points, P1/P24 labels, leader scale, and trend rows. Preview evidence should not mix unavailable status with stale-looking fixture graph content without explicit provenance.
- Evidence gap: race aggregate counts exist, but sampled diagnostic frames are Warmup-only, so we cannot inspect concrete race-phase Gap rows.
- IBT sidecar limitation: the mismatched Porsche/Spa IBT lacks Gap-critical live array fields such as `CarIdxF2Time`, `CarIdxLapDistPct`, `CarIdxPosition`, `CarIdxClassPosition`, `CarIdxEstTime`, `CarIdxLapCompleted`, and `CamCarIdx`.

#### Track Map

- Evidence gap / product concern: `metadata/track-maps.json` reports the current track key `track-264-nurburgring-combinedlong-gesamtstrecke-long-25655m-2026-03-30-02`, `hasReadableMap: false`, and fallback reason `no_matching_runtime_map`. There is no readable user or bundled Nurburgring map for this capture.
- Evidence gap / product confusion: all Track Map previews are marked `status: live` and `evidence: Live`, but the manifest vector reports `mapKind: circle`. The preview can make fallback geometry look like a valid live map.
- Diagnostics issue: the latest track-map generation event for `capture-20260519-144859-079` was skipped as `complete_map_already_exists`, but the source file was the mismatched Porsche/Spa IBT and the existing complete map is Spa, not current Nurburgring.
- Capture evidence: diagnostics show `trackMap.framesWithSectors: 194032` and `framesWithLiveTiming: 194032`, but `framesWithHighlightedSectors: 0`, `personalBestSectorFrames: 0`, `bestLapSectorFrames: 0`, and no current real map document. V1.0.3 needs to prove map selection, generation, reload, and fallback labelling.

#### Car Radar

- Capture-data limitation: the latest live capture cannot validate real Radar behavior. Diagnostics show `radar.framesWithData: 0`, `nonPlayerFocusFrames: 194001`, `localUnavailablePitOrGarageFrames: 194032`, `sideSignalFrames: 0`, `multiclassApproachFrames: 0`, and no side transitions.
- Evidence gap: `car-radar-practice.png` and `car-radar-qualifying.png` show `faster class`, and `car-radar-race.png` shows a right-side car, but these are forced preview fixtures. The real capture is effectively a single race class, aside from pace-car/spectator data, and radar diagnostics have no live side evidence.
- Product/preview concern: the race preview status is `car right`, but the footer label still includes `Faster class approaching 2.4s`. If this is intentional combined fixture coverage, the manifest should expose it as synthetic mixed-state evidence.

#### Flags

- Evidence gap: Flags previews are fixture flags, not captured race flags. Practice shows Blue, Qualifying shows Yellow + Blue, and Race shows Yellow + Blue + Checkered; live diagnostics show display kinds Checkered, Green, and Yellow, with no Blue.
- Diagnostics wording issue: `framesWithActiveRawFlags: 194032` and `rawActiveWithoutDisplayFrames: 177344` are noisy because most raw-active samples map `0x10000000` to a start-hidden/non-display flag. This should be reported as suppressed non-display flag state, not as suspicious active flag loss.
- Existing V1.0.2 flag coverage remains useful, but this capture does not prove ordinary yellow, debris, waving yellow, random waving, caution, one-to-green, or blue handling in live renderers.

#### Input State

- Product bug: Input State previews render unavailable despite valid fixture inputs. The preview manifest shows `status: waiting for fresh telemetry`, `evidence: Unavailable`, `inputs.isAvailable: false`, `tracePointCount: 0`, and readouts like `THR --`, `GEAR --`, and `SPD --`, while `SessionPreviewState` supplies on-track speed, gear, rpm, throttle, brake, clutch, steering, and ABS state.
- Capture-data limitation: the real capture cannot validate on-track Input State behavior. Sampled control signals are effectively stationary: steering and throttle are 0, brake is almost always 1, gear is neutral in sampled frames, and speed is 0.
- Loose pixel evidence: `temamate-inputs.png` shows the native Inputs panel visible in a game scene with blank trace bands and an ABS/pit-lane state, but no manifest ties it to the capture frame, route, or model state. Add manifest-backed native pixel capture before treating it as parity proof.

#### Stream Chat

- Evidence gap: Stream Chat previews intentionally suppress external chat by forcing the provider to none. The preview PNGs show `waiting for chat source`, `evidence: Unavailable`, and `streamChatExternalSourceSuppressed: true`.
- Evidence gap: actual settings/metadata show Twitch configured and connected to `techmatesracing` with one visible system message and no errors, and the live overlay manifest says Stream Chat was actual-visible. There is still no live pixel proof because screenshot capture was disabled.

#### Garage Cover

- Evidence gap: Garage Cover is present in browser overlay metadata, but absent from the live overlay manifest, preview manifest, and preview PNGs. V1.0.3 needs an explicit product decision on whether it is a first-class overlay for browser/native/localhost evidence or only future/hidden browser metadata.

### Performance And Runtime Findings

- `metadata/performance.json` shows over-budget `overlay.gap-to-leader.design_v2.paint` with p95 around 27.752 ms and max around 48.198 ms against a 16.667 ms frame budget.
- `overlay.stream_chat.connect` recorded a 216.289 ms connect sample against a 50 ms budget. That may be acceptable as an IO event, but the budget classification should be explicit.
- `telemetry.finalize_collection` recorded around 15422.617 ms against a 50 ms budget. This is likely misclassified as a frame-scale budget, but the 15.4 s finalization cost is real evidence to track.
- Process memory after capture was high: working set around 2.39 GB, private memory around 2.32 GB, and managed heap around 2.14 GB.
- `localhost.request` had 280410 requests, p95 around 0.167 ms, and max around 233 ms. The route was responsive, but the polling volume is high enough to keep in V1.0.3 performance validation.
- `overlay.manager.apply_settings` had 3717 samples with p95 around 16.38 ms. It is technically within budget, but the count is high enough to investigate if repeated apply cycles are still being triggered.
- `runtime/runtime-state.json` shows `version: 1.0.2.0` and `stoppedCleanly: false` because the app was still running when captured. The log also says the previous 1.0.1 run did not stop cleanly before the update.

### Validation Work To Add

- Add manifest validation that fails when preview screenshots are labeled `Live` or `Measured` but are sourced from `SessionPreviewTelemetryFixtures` without an explicit synthetic/capture-specific provenance field.
- Add live overlay manifest validation that fails when an actual-visible or desired-visible overlay lacks current pixel evidence, unless the bundle explicitly declares screenshot capture disabled as a blocking evidence limitation.
- Add localhost/browser/native parity manifest validation for every supported overlay route, not just last-observed localhost request stats.
- Add shared settings parity validation so browser review, localhost, and native prove the same overlay setting fingerprint. This should catch the class of regressions where localhost kept redundant overlay titles or Standings/Relative row settings diverged even though the product settings looked shared.
- Add table data-contract validation for row identity, placeholder rows, and column keys, not just row count. This should catch Standings/Relative mismatches where rows are clipped, padded, or rendered from a different effective setting.
- Add explicit localhost data-contract regression checks for redundant overlay titles, per-route evidence, and native/browser/localhost shared-settings hashes. V1.0.2 was too easy to mark complete while localhost still carried chrome and different row settings than the native/browser surfaces.
- Add IBT sidecar validation that fails `succeeded` or downgrades the evidence when the selected IBT car, track, date/time, or source filename does not match the latest capture.
- Add per-session diagnostic sample validation so Race captures include concrete Race sample frames even when Warmup happens first.
- Add Track Map validation that distinguishes circle fallback from a real readable map and fails if a current-track map generation skip references a different track's IBT/map.
- Add overlay-specific failing checks for the product bugs above: Standings visible-row/status mismatch, impossible Standings interval, Fuel `Covered` with null need, Fuel/Pit collapsed-content oversized shells, Input preview unavailable despite fixture inputs, and Gap preview graph content while unavailable.
- Add evidence-gap checks for Radar, Flags, Stream Chat, Garage Cover, and loose/native screenshots so the support bundle says exactly which surfaces are unproven instead of letting synthetic previews appear authoritative.

## Overlay Visibility Modes

Add overlay visibility controls for driver state:

- in car
- out of car
- spotting
- garage

Evaluate whether these should replace or take priority over the current practice / qualifying / race visibility settings. The goal is to let users express when an overlay is useful by real usage context instead of only by session type.

Open decisions:

- Whether session-type visibility should remain as an advanced filter, become a secondary condition, or be retired.
- Which telemetry signals reliably distinguish in-car, out-of-car, spotting/spectating, and garage/setup states. As of the GR86 spotter-only capture, the strongest spotting signal is session-info identity (`DriverInfo.DriverCarIdx` resolves to a driver row with `IsSpectator = 1`), not a new raw SDK boolean. Raw `IsReplayPlaying` can be true during active spotting/watch contexts and should be treated as camera/replay plumbing unless paired with stronger context.
- How each overlay should default across those states, especially Pit Service, Inputs, Track Map, Car Radar, Garage Cover, and Stream Chat.
