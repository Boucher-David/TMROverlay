# Fuel V2 Bottom-Half Real-History References

`manifest.json` is a compact, sanitized extraction of three local real V1
historical-session summaries. It is deliberately not a copy of raw telemetry,
an old V1 summary file, or a manufactured Fuel V2 format-2 sidecar.

Use it to select and discuss deterministic browser-workbench rows, and later to
write focused bottom-half characterization tests. Preserve the stated
assertions, evidence gaps, and service classification when adapting a case.

Do not import these files through `FuelV2HistoryImporter` or treat them as
classified learned history. Format-2 intake must be proven separately from real
format-2 sidecars produced by the live recorder.
